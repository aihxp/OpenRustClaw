---
phase: 183-learning-candidate-review-and-lesson-promotion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/types.rs
  - crates/db/src/models.rs
  - crates/db/src/migrate.rs
  - crates/db/src/lib.rs
  - crates/db/src/learning_store.rs
  - crates/app/src/learning_review.rs
  - crates/app/src/lib.rs
autonomous: true
requirements:
  - LEAR-01
  - LEAR-02
  - LEAR-04
must_haves:
  truths:
    - "Learning candidates exist as durable typed records with provenance, evidence, and explicit review state."
    - "Candidate review and promotion gating live behind one app-layer service instead of direct CLI file writes."
    - "Higher-impact candidates cannot promote without linked review evidence."
  artifacts:
    - path: "crates/core/src/types.rs"
      provides: "Shared learning-candidate contracts"
      contains: "candidate kind, review state, provenance, evidence, and promotion types"
    - path: "crates/db/src/learning_store.rs"
      provides: "Durable candidate queue and evidence storage"
      contains: "candidate CRUD, review transitions, and promotion history persistence"
    - path: "crates/app/src/learning_review.rs"
      provides: "Review and promotion policy seam"
      contains: "candidate ingestion, approval, rejection, supersede, and rollback logic"
  key_links:
    - from: "crates/app/src/learning_review.rs"
      to: "crates/db/src/learning_store.rs"
      via: "candidate lifecycle persistence"
      pattern: "candidate|review|promotion"
    - from: "crates/app/src/learning_review.rs"
      to: "crates/app/src/autonomy_lessons_control.rs"
      via: "approved promotion into bounded active lessons"
      pattern: "lesson"
---

<objective>
Introduce the durable candidate-review layer that sits between reflection output and live lessons.

Purpose: Phase 183 cannot be trustworthy if successful work jumps straight into active runtime guidance without durable review state and evidence.
Output: shared candidate types, SQLite-backed queue and evidence storage, and an app service that enforces candidate lifecycle and promotion rules.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-learning-candidate-review-and-lesson-promotion/183-CONTEXT.md
@.planning/phases/183-learning-candidate-review-and-lesson-promotion/183-RESEARCH.md
@crates/app/src/orchestration_reporting.rs
@crates/app/src/autonomy_lessons_control.rs
@crates/app/src/tool_execution_audit.rs
@crates/db/src/migrate.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared learning-candidate, evidence, and promotion contracts</name>
  <files>crates/core/src/types.rs, crates/app/src/learning_review.rs, crates/app/src/lib.rs</files>
  <behavior>
    - Test 1: learning-candidate and review-status types serialize cleanly.
    - Test 2: high-impact candidates require evidence metadata before promotion is allowed.
    - Test 3: rollback and supersede reports preserve candidate and lesson linkage.
  </behavior>
  <action>Add shared types for candidate identity, provenance, evidence, review state, promotion requests, and rollback results. Create a dedicated app-layer review service that owns lifecycle rules and exposes bounded operations for ingestion, review, promotion, and rollback.</action>
  <verify>
    <automated>cargo test -p openrustclaw-core --lib</automated>
    <automated>cargo test -p openrustclaw-app learning_review -- --nocapture</automated>
  </verify>
  <done>Phase 183 has one typed contract for candidate review instead of ad hoc JSON and direct lesson writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add durable candidate, evidence, and promotion-history storage</name>
  <files>crates/db/src/models.rs, crates/db/src/migrate.rs, crates/db/src/lib.rs, crates/db/src/learning_store.rs</files>
  <behavior>
    - Test 1: candidates round-trip with provenance, confidence, status, and timestamps.
    - Test 2: evidence rows link cleanly to candidates and remain inspectable after review transitions.
    - Test 3: promotion and rollback history preserve candidate-to-lesson lineage.
  </behavior>
  <action>Add SQLite-backed candidate, evidence, and promotion-history tables plus a `learning_store` module that lists, fetches, transitions, and records promotion outcomes. Reuse established review-queue patterns from adjacent DB code while keeping the active lesson registry unchanged.</action>
  <verify>
    <automated>cargo test -p openrustclaw-db learning_store -- --nocapture</automated>
  </verify>
  <done>The runtime has a durable candidate-review source of truth with explicit lineage and evidence.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01-01 | T/R | candidate promotion | mitigate | Require typed review state and promotion history before lesson activation |
| T-183-01-02 | I | candidate evidence | mitigate | Persist source refs and evidence attachments separately from summary text |
| T-183-01-03 | E | high-impact learning | mitigate | Block promotion unless replay or equivalent review evidence exists |
</threat_model>

<verification>
- `cargo test -p openrustclaw-core --lib`
- `cargo test -p openrustclaw-db learning_store -- --nocapture`
- `cargo test -p openrustclaw-app learning_review -- --nocapture`
</verification>
