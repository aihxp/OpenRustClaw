# Plan 50-01: Harden the Docs.rs Surface for `openrustclaw-core`

## Objective

Define and verify the docs.rs build contract for the first public OpenRustClaw crate.

## Steps

1. Add explicit docs.rs build metadata to the `openrustclaw-core` manifest.
2. Improve the crate root rustdoc so docs.rs has a useful public landing page.
3. Verify standard and docs.rs-style documentation builds locally.

## Verification

- `cargo doc -p openrustclaw-core --no-deps`
- `RUSTDOCFLAGS="--cfg docsrs" cargo doc -p openrustclaw-core --no-deps`
