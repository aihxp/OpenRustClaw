# Plan 46-01: Repair Linux Release Workflow Dependencies

## Objective

Make the Linux build jobs in `release-binaries.yml` stop failing on missing system package setup.

## Steps

1. Add the missing native Linux ALSA development package to the workflow.
2. Add the ARM64 sysroot packages and pkg-config or OpenSSL environment needed for cross-compilation.
3. Extend the admin helper and release docs so operators can validate the release workflow deliberately.
4. Push a checkpoint and run `workflow_dispatch` on GitHub to validate the Linux build matrix on real runners.

## Verification

- `bash -n scripts/github-actions-admin.sh`
- `python3 - <<'PY' ... yaml.safe_load('.github/workflows/release-binaries.yml') ... PY`
- `bash scripts/github-actions-admin.sh check-main-ci`
- `gh workflow run release-binaries.yml --ref <branch-or-main>`
