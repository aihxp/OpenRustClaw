# Plan 53-01: Define the Node and Topology Contract

## Objective

Make the current node and remote-connectivity model explicit across the shipped docs and onboarding surfaces.

## Steps

1. Define the current remote-connectivity order and node terminology in one canonical mdBook page.
2. Update README, introduction, production docs, and distributed crate docs so they reference the same contract.
3. Replace the vague onboarding guidance with the explicit node-first, SSH tunnel fallback, reverse-proxy last-resort wording.

## Verification

- `cargo test -p openrustclaw-cli onboard -- --nocapture`
- `mdbook build docs`
