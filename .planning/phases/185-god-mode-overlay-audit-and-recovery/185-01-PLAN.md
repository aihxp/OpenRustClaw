---
phase: 185-god-mode-overlay-audit-and-recovery
plan: "01"
type: execute
wave: 1
depends_on:
  - "184-02"
files_modified:
  - crates/cli/src/commands/enterprise_autonomy.rs
  - crates/cli/src/commands/inspect.rs
  - crates/cli/src/commands/control_ui.html
  - crates/app/src/enterprise_admin.rs
autonomous: true
requirements:
  - GOD-01
  - GOD-02
  - GOD-04
must_haves:
  truths:
    - "God Mode is a first-class named operator lane layered on the existing enterprise autonomy seam."
    - "Enable, disable, kill-switch, and expiry all restore the saved baseline runtime policy."
    - "Operator-facing summaries describe the stronger lane as God Mode and surface expiry metadata."
  artifacts:
    - path: "crates/cli/src/commands/enterprise_autonomy.rs"
      provides: "God Mode manifest, TTL, expiry, and baseline restore logic"
      contains: "mode label, scope, expires_at, expiry events, and run labeling"
    - path: "crates/cli/src/commands/inspect.rs"
      provides: "God Mode inspection summaries"
      contains: "enterprise admin and autonomy reporting that surfaces the named lane"
    - path: "crates/cli/src/commands/control_ui.html"
      provides: "Operator-facing God Mode controls"
      contains: "God Mode labels, TTL-aware copy, and stronger recovery messaging"
  key_links:
    - from: "crates/cli/src/commands/enterprise_autonomy.rs"
      to: "crates/cli/src/commands/control.rs"
      via: "baseline capture and runtime-autonomy restore"
      pattern: "runtime_autonomy|set_runtime_autonomy"
    - from: "crates/cli/src/commands/inspect.rs"
      to: "crates/app/src/enterprise_admin.rs"
      via: "named stronger-lane presentation"
      pattern: "God Mode|autonomy"
---

<objective>
Turn the existing full-autonomy override into an explicit God Mode control surface with expiry and recovery guarantees.

Purpose: Phase 185 cannot close if the stronger lane still behaves like an unlabeled yolo override that depends on manual disable.
Output: manifest/report TTL support, expiry enforcement, baseline restore on all exits, and operator-facing God Mode naming updates.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-god-mode-overlay-audit-and-recovery/185-CONTEXT.md
@.planning/phases/185-god-mode-overlay-audit-and-recovery/185-RESEARCH.md
@.planning/phases/185-god-mode-overlay-audit-and-recovery/185-VALIDATION.md
@crates/cli/src/commands/enterprise_autonomy.rs
@crates/cli/src/commands/control.rs
@crates/cli/src/commands/inspect.rs
@crates/app/src/enterprise_admin.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add God Mode manifest fields, TTL expiry, and baseline restore enforcement</name>
  <files>crates/cli/src/commands/enterprise_autonomy.rs</files>
  <behavior>
    - Test 1: enable persists mode label, scope, and expiry metadata.
    - Test 2: expired God Mode restores the baseline runtime policy and records an expiry event.
    - Test 3: kill-switch still restores baseline and marks matching active runs.
  </behavior>
  <action>Extend the enterprise autonomy manifest and report with first-class God Mode metadata, TTL support, and expiry refresh logic. Keep the existing governance scope and route family, but make the stronger lane explicit and self-restoring.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli enterprise_autonomy -- --nocapture</automated>
  </verify>
  <done>God Mode becomes an explicit stronger lane that cannot remain enabled indefinitely by accident.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update inspect and operator-facing summaries to describe God Mode directly</name>
  <files>crates/cli/src/commands/inspect.rs, crates/cli/src/commands/control_ui.html, crates/app/src/enterprise_admin.rs</files>
  <behavior>
    - Test 1: inspect and enterprise admin summaries mention God Mode instead of only full autonomy.
    - Test 2: control UI shows God Mode labels and expiry-aware recovery copy.
    - Test 3: report payloads retain route-compatible fields while exposing the stronger-lane metadata.
  </behavior>
  <action>Update the enterprise admin presentation, inspect summaries, and control UI to name and explain God Mode directly while preserving the existing control surface contracts.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli inspect -- --nocapture</automated>
  </verify>
  <done>Operators can see when they are using God Mode without inferring it from low-level autonomy settings.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-01-01 | E | God Mode lifetime | mitigate | Persist TTL and enforce expiry with baseline restore |
| T-185-01-02 | R | operator understanding | mitigate | Expose first-class God Mode labels and recovery copy in reports and UI |
| T-185-01-03 | D | baseline restore | mitigate | Reuse the existing runtime autonomy policy seam for every exit path |
</threat_model>

<verification>
- `cargo test -p openrustclaw-cli enterprise_autonomy -- --nocapture`
- `cargo test -p openrustclaw-cli inspect -- --nocapture`
</verification>
