# Plan 20-02: Enforce Approval Chains For Sensitive Enterprise Writes

## Goal

Make higher-risk enterprise writes require the stronger governance contract instead of the current flat scope-only check.

## Must Have

- requester roles are validated for governed scopes
- configured dual-approval scopes require a second operator identity and reject self-approval
- the enterprise middleware and route tests prove the stronger contract is active on sensitive writes

## Expected Files

- `crates/cli/src/commands/enterprise_access.rs`
- `crates/cli/src/commands/start.rs`

## Verification

- targeted enterprise access and middleware tests for denied single-operator writes and allowed dual-approved writes
