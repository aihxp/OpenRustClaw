---
phase: 10
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
autonomous: true
user_setup: []
requirements:
  - ENTF-01
  - ENTF-03
must_haves:
  truths:
    - Control UI exposes the enterprise baseline as a first-class operator panel.
    - The panel communicates both the approval contract and recent audit evidence in plain operator language.
    - The shipped UI tests lock the new enterprise foundations surface into the dashboard contract.
  artifacts:
    - path: crates/cli/src/commands/control_ui.html
      purpose: Operator-facing enterprise foundations panel and loader
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Dashboard contract test coverage for the new panel
---

# Phase 10 Plan 02: Surface the enterprise baseline in Control UI

## Objective
- **What:** Add an enterprise foundations panel to Control UI.
- **Why:** A runtime endpoint alone is not enough if the primary operator surface still hides the baseline behind raw JSON discovery.
- **Output:** Control UI shows policy boundary plus recent audit evidence for approval-sensitive actions.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-enterprise-policy-and-audit-foundations/10-CONTEXT.md

## Tasks

<task type="code">
  <name>Add enterprise foundations panel</name>
  <files>crates/cli/src/commands/control_ui.html</files>
  <action>Create a dedicated panel and loader that fetches and renders the enterprise foundations summary in the dashboard.</action>
  <verify>Operators can see the approval boundary and recent evidence without navigating raw lower-level endpoints manually.</verify>
  <done>The dashboard exposes enterprise foundations as a first-class inspection surface.</done>
</task>

<task type="test">
  <name>Lock the dashboard contract</name>
  <files>crates/cli/src/commands/control_ui.rs</files>
  <action>Add a focused dashboard test for the enterprise foundations panel and loader function.</action>
  <verify>The panel presence is covered by a stable UI contract test.</verify>
  <done>The dashboard test suite protects the new operator surface.</done>
</task>

## Verification
Run the focused Control UI dashboard test for the enterprise foundations panel.

## Success Criteria
- [ ] Control UI includes an enterprise foundations panel.
- [ ] The panel loads data from the shipped summary endpoint.
- [ ] The dashboard contract test covers the new surface.
