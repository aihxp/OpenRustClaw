# Plan 58-01: Create the Greenfield Core Shell and Service Boundaries

## Objective

Add the first application-layer crate and define the stable service interface for the setup handoff proving slice.

## Steps

1. Create a new workspace crate for greenfield application services and wire it into the workspace metadata.
2. Define the setup-handoff domain state, report output, and source trait in the new crate.
3. Add unit tests that prove default reporting and bounded state-to-report transformation without depending on CLI-specific command modules.

## Verification

- `cargo test -p openrustclaw-app -- --nocapture`
