---
phase: 01
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/cli/src/commands/onboard.rs
  - crates/cli/src/commands/doctor.rs
  - tests/integration/src
autonomous: true
user_setup: []
requirements:
  - ONBD-02
  - ONBD-03
must_haves:
  truths:
    - The onboarding path has regression coverage beyond helper-level unit tests.
    - First-run health gating and assistant handoff behavior are exercised through workflow-level tests.
    - Existing workspace-state choices and post-setup outcomes are covered well enough to trust refactors.
  artifacts:
    - path: tests/integration/src
      purpose: Workflow-level regression coverage for onboarding and doctor behavior
    - path: crates/cli/src/commands/onboard.rs
      purpose: Test seams or supporting behavior needed for reliable onboarding verification
  key_links:
    - from: tests/integration/src
      to: crates/cli/src/commands/onboard.rs
      via: tests exercise onboarding workspace-state and post-check flow
---

# Phase 1 Plan 02: Add workflow-level onboarding regression coverage

## Objective
- **What:** Add stronger workflow-level coverage for onboarding and post-setup health gating.
- **Why:** Phase 1 is not production-ready if the first-run path only has helper-level unit tests.
- **Output:** Integration-style tests that exercise onboarding choices, doctor-backed readiness outcomes, and persisted assistant handoff conditions.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-onboarding-and-first-run-trust/01-CONTEXT.md
@.planning/phases/01-onboarding-and-first-run-trust/01-01-SUMMARY.md

## Tasks

<task type="code">
  <name>Add onboarding workflow test seams where necessary</name>
  <files>crates/cli/src/commands/onboard.rs, crates/cli/src/commands/doctor.rs</files>
  <action>Refactor narrowly so onboarding post-check and launch gating can be exercised without interactive terminal prompts.</action>
  <verify>New tests can drive readiness outcomes without relying on live prompts.</verify>
  <done>Onboarding workflow behavior is testable without distorting the shipped CLI surface.</done>
</task>

<task type="code">
  <name>Add workflow-level regression tests for onboarding outcomes</name>
  <files>tests/integration/src, crates/cli/src/commands/onboard.rs, crates/cli/src/commands/doctor.rs</files>
  <action>Create focused regression tests for existing-state handling, post-setup readiness outcomes, and assistant launch gating after onboarding.</action>
  <verify>Targeted integration tests pass and fail if onboarding regresses into offering broken first starts.</verify>
  <done>Workflow-level coverage exists for the main onboarding trust path.</done>
</task>

## Verification
Run the new onboarding or doctor integration test targets and confirm they cover both ready and blocked first-start outcomes.

## Success Criteria
- [ ] Onboarding workflow behavior is covered beyond helper-only tests.
- [ ] The assistant handoff after onboarding is tested for both ready and blocked states.
- [ ] Existing-workspace choices are covered by regression tests.
