# Plan 51-01: Verify the First Crates.io Publish Path

## Objective

Turn the selected public crate into one repeatable publish workflow with dry-run evidence and operator-facing documentation.

## Steps

1. Add a rerunnable readiness script for metadata, package, docs, and publish dry-run verification.
2. Document the operator runbook for credentials, publish order, publish execution, and post-publish checks.
3. Run the dry-run verification bundle for `openrustclaw-core`.

## Verification

- `bash scripts/check-crates-io-readiness.sh openrustclaw-core`
- `mdbook build docs`
