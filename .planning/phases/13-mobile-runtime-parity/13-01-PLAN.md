---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/mobile.rs
  - tests/integration/src/mobile_operator_report_test.rs
  - tests/integration/src/lib.rs
requirements:
  - MOBL-01
  - MOBL-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - A typed mobile operator report exposes broader per-node runtime state than scattered raw mobile receipts alone.
    - The richer mobile report preserves approval, conflict, notification, and message evidence instead of collapsing them into opaque status.
    - Mobile parity stays rooted in existing durable receipts and summaries.
  artifacts:
    - path: crates/cli/src/commands/mobile.rs
      purpose: Add typed mobile operator report helpers and attention-signal aggregation
    - path: tests/integration/src/mobile_operator_report_test.rs
      purpose: Prove the richer report captures approval-sensitive and conflict-sensitive mobile state
  key_links:
    - from: mobile receipts and runtime state
      to: typed operator report
      via: Rust-owned aggregation over existing mobile summaries and timelines
---

# Phase 13 Plan 01: Add typed mobile operator reports

## Objective
- **What:** Expose a richer typed mobile operator report built from the existing mobile runtime and receipt surfaces.
- **Why:** Phase 13 cannot claim broader mobile parity while operators still need to manually correlate runtime health, approval pressure, sync conflicts, and recent activity across many separate mobile endpoints.
- **Output:** Typed mobile operator report helpers plus focused integration coverage.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-mobile-runtime-parity/13-CONTEXT.md

## Tasks

<task type="code">
  <name>Expose richer mobile node operator report details</name>
  <files>crates/cli/src/commands/mobile.rs</files>
  <action>Add a typed per-node mobile operator report that surfaces runtime health, push/sync state, receipt counts, recent activity, and operator attention signals from existing durable data.</action>
  <verify>Operators can inspect one coherent per-node mobile runtime report instead of manually stitching multiple raw slices together.</verify>
  <done>The mobile runtime has a stable operator-readable report contract.</done>
</task>

<task type="code">
  <name>Cover the mobile report with focused integration evidence</name>
  <files>tests/integration/src/mobile_operator_report_test.rs, tests/integration/src/lib.rs</files>
  <action>Add focused integration coverage proving the richer mobile report captures approval-sensitive commands, sync conflicts, and recent activity signals.</action>
  <verify>The new report is backed by realistic mobile fixture flows rather than only unit assembly tests.</verify>
  <done>Phase 13 gains durable evidence for the richer mobile report contract.</done>
</task>

## Verification
Run focused mobile integration and CLI tests covering the new operator report.

## Success Criteria
- [ ] One typed mobile operator report exposes broader per-node runtime state directly.
- [ ] Approval, sync-conflict, and receipt signals remain visible in the richer report.
- [ ] The report is backed by focused integration coverage.
