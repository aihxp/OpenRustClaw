---
phase: 13
plan: 02
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
requirements:
  - MOBL-01
  - MOBL-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operators can fetch and inspect the richer mobile report from shipped runtime surfaces.
    - The main mobile inspection surface in Control UI uses the typed report for node-level understanding instead of raw JSON alone.
    - Mobile actions remain anchored to the same approval-aware runtime contracts.
  artifacts:
    - path: crates/cli/src/commands/start.rs
      purpose: Expose typed mobile operator report routes
    - path: crates/cli/src/commands/control_ui.html
      purpose: Render the richer mobile report in the shipped dashboard
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Lock the mobile report rendering contract
  key_links:
    - from: mobile operator report route
      to: main mobile inspection surface
      via: direct dashboard fetch and render
---

# Phase 13 Plan 02: Surface mobile operator reports in runtime APIs and Control UI

## Objective
- **What:** Wire the richer mobile operator report into shipped runtime routes and the main mobile inspection surface.
- **Why:** Typed mobile reports only close the parity gap if operators can access them from the same runtime and dashboard they already use.
- **Output:** Updated mobile runtime handlers plus selective Control UI rendering for the richer mobile report.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-mobile-runtime-parity/13-CONTEXT.md

## Tasks

<task type="code">
  <name>Expose the mobile operator report through shipped runtime routes</name>
  <files>crates/cli/src/commands/start.rs</files>
  <action>Add a stable `/control/mobile/nodes/{id}/summary` or equivalent typed route for the richer mobile operator report.</action>
  <verify>Operators can fetch the broader mobile report without direct filesystem access.</verify>
  <done>The runtime exposes a coherent mobile operator-report surface.</done>
</task>

<task type="code">
  <name>Render the richer mobile report in the main node inspection view</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Use the typed mobile operator report to render the primary node inspection surface more clearly, while leaving the broader multi-pane mobile dashboard cleanup for Phase 14.</action>
  <verify>The main mobile node view becomes materially easier to understand without abandoning existing control actions.</verify>
  <done>The shipped dashboard exposes deeper mobile parity through the typed report.</done>
</task>

## Verification
Run focused mobile dashboard and report tests after the runtime/UI surfaces are wired.

## Success Criteria
- [ ] Runtime endpoints expose the richer mobile operator report.
- [ ] The main mobile node inspection view renders the report clearly.
- [ ] Mobile controls remain backed by the same typed runtime contracts.
