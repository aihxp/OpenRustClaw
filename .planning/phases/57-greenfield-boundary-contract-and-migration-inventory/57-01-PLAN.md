# Plan 57-01: Define the Architecture Boundary and Migration Inventory

## Objective

Create one canonical contract for the brownfield-to-greenfield transition and choose the first migration slice explicitly.

## Steps

1. Write the planning-facing greenfield boundary contract, including layer ownership, dependency direction, no-touch legacy seams, and ranked migration targets.
2. Add a contributor-facing architecture page and update the mdBook navigation so the new lane is visible from the existing architecture docs.
3. Update development guidance so new code defaults to the greenfield lane and names the first proving slice plus its baseline verification bundle.

## Verification

- `cargo test -p openrustclaw-cli setup_handoff_summary -- --nocapture`
- `cargo test -p openrustclaw-cli dashboard_includes_setup_handoff_panel -- --nocapture`
- `mdbook build docs`
