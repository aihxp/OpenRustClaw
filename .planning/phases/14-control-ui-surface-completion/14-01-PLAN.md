---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
requirements:
  - CTRL-01
  - CTRL-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - The highest-value remaining raw-detail panes are explicitly identified and upgraded first.
    - Control UI reduces raw JSON dependence on operator-critical surfaces instead of adding more low-value chrome.
  artifacts:
    - path: crates/cli/src/commands/control_ui.html
      purpose: Add typed renderers for the first batch of high-value raw detail panes
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Lock the new renderer contracts
---

# Phase 14 Plan 01: Upgrade the first batch of high-value raw detail panes

## Objective
- **What:** Replace the most operator-important remaining raw JSON panes with typed renderers.
- **Why:** The Control UI cannot claim deeper parity while core trust-sensitive details still read like developer dumps.
- **Output:** First batch of typed renderers plus dashboard contract coverage.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-control-ui-surface-completion/14-CONTEXT.md

## Tasks

<task type="code">
  <name>Upgrade remaining high-value detail panes</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Replace the highest-value remaining raw panes with compact summaries, tables, or timelines backed by existing typed payloads.</action>
  <verify>Operators can understand those surfaces without reading raw JSON dumps.</verify>
  <done>The dashboard takes a real step toward parity on operator-critical detail panes.</done>
</task>

## Verification
Run focused dashboard contract tests for the upgraded panes.

## Success Criteria
- [ ] The first batch of high-value raw panes are upgraded.
- [ ] The upgraded panes remain backed by existing typed runtime payloads.
