# Plan 19-01: Add A Typed Enterprise Admin Summary

## Goal

Expose one typed enterprise admin report that combines access, policy, and supervised-runtime attention state for Control UI.

## Must Have

- one runtime report combines enterprise access and policy state
- supervised-runtime attention counts are included so enterprise operators can see what still needs review
- the new summary reuses existing typed subsystems instead of inventing parallel state

## Expected Files

- `crates/cli/src/commands/inspect.rs`
- `crates/cli/src/commands/start.rs`

## Verification

- targeted inspect and route-facing tests for the combined summary
