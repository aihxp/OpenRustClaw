---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/channels/src/gmail_pubsub.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/control_ui.html
  - tests/integration/src
autonomous: true
user_setup: []
requirements:
  - COMM-01
must_haves:
  truths:
    - Recent email ingress and action outcomes are retained as durable operator evidence instead of disappearing into webhook logs.
    - Operators can inspect recent email outcomes from shipped runtime surfaces.
    - Gmail readiness and failure behavior are clear enough for production debugging.
  artifacts:
    - path: crates/channels/src/gmail_pubsub.rs
      purpose: Persist structured email workflow receipts and outcome metadata
    - path: crates/cli/src/commands/start.rs
      purpose: Expose recent email activity through the control plane
    - path: crates/cli/src/commands/control_ui.html
      purpose: Browser operator surface for recent email outcomes
    - path: tests/integration/src
      purpose: Regression coverage for the retained email audit path
  key_links:
    - from: crates/channels/src/gmail_pubsub.rs
      to: crates/cli/src/commands/start.rs
      via: durable email receipts become inspectable control-plane reports
---

# Phase 5 Plan 01: Add durable operator evidence for email workflows

## Objective
- **What:** Make the Gmail Pub/Sub communication lane leave behind operator-reviewable receipts for ingress and action outcomes.
- **Why:** Phase 5 cannot claim MVP-ready email support if recent email behavior is only visible through logs, raw channel internals, or ephemeral webhook handling.
- **Output:** A durable email activity trail plus control-surface visibility for recent email workflow outcomes.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-email-and-voice-communications/05-CONTEXT.md

## Tasks

<task type="code">
  <name>Persist structured email workflow receipts</name>
  <files>crates/channels/src/gmail_pubsub.rs</files>
  <action>Capture recent inbound email notifications and action outcomes in a durable, reviewable form that preserves message identity, workflow action, and failure detail.</action>
  <verify>Email ingress and action flows leave behind stable receipts even when the operator was not watching the live runtime logs.</verify>
  <done>The shipped Gmail lane has a truthful recent-activity trail.</done>
</task>

<task type="code">
  <name>Expose recent email outcomes through control inspection</name>
  <files>crates/cli/src/commands/start.rs, crates/cli/src/commands/control_ui.html</files>
  <action>Add control endpoints and UI visibility for recent email communication outcomes and their artifact locations.</action>
  <verify>Operators can inspect recent email activity without reading raw workspace files.</verify>
  <done>Email communication trust is visible from shipped operator surfaces.</done>
</task>

<task type="code">
  <name>Lock the email audit path with automation</name>
  <files>tests/integration/src</files>
  <action>Add focused tests that prove Gmail workflow receipts persist and remain visible through the intended inspection surface.</action>
  <verify>Tests fail if recent email outcomes stop being retained or inspectable.</verify>
  <done>The email MVP contract is backed by regression coverage.</done>
</task>

## Verification
Run targeted tests that drive representative Gmail ingress or action flows and confirm the resulting receipts are retained and exposed through the intended operator surface.

## Success Criteria
- [ ] Gmail workflow activity persists as durable operator evidence.
- [ ] Recent email outcomes are visible through shipped control surfaces.
- [ ] Gmail readiness and failure behavior are inspectable enough for debugging.
