# Plan 55-01: Surface the Remote Connectivity Profile in Control UI

## Objective

Make the saved node-first and fallback contract visible from the shipped operator dashboard.

## Steps

1. Extend the setup handoff renderer to show the primary remote path and fallback order.
2. Expose the saved remote-connectivity detail alongside the existing blockers and pending-step guidance.
3. Lock the dashboard coverage with a focused Control UI test.

## Verification

- `cargo test -p openrustclaw-cli setup_handoff_summary -- --nocapture`
- `cargo test -p openrustclaw-cli dashboard_includes_setup_handoff_panel -- --nocapture`
