---
phase: 15
plan: 02
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
requirements:
  - VOIC-01
  - VOIC-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Control UI renders the richer voice and call operator story without forcing operators back into raw `pre` blocks.
    - The dashboard stays grounded in the same typed report and existing typed voice payloads.
  artifacts:
    - path: crates/cli/src/commands/control_ui.html
      purpose: Render the voice operator report and upgrade remaining top-level voice panes
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Lock the new voice parity renderer contracts
---

# Phase 15 Plan 02: Surface the voice operator report in Control UI

## Objective
- **What:** Upgrade the top-level voice and call dashboard panes to consume the new report and stronger typed renderers.
- **Why:** The parity gain is only real if operators can use it from the shipped Control UI.
- **Output:** A more coherent voice and call operator surface in `/control/ui`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-voice-and-call-handling-parity/15-CONTEXT.md

## Tasks

<task type="code">
  <name>Render the voice operator report</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Add typed voice and call operator summary rendering, attention tables, and recent-activity surfaces while keeping existing voice actions intact.</action>
  <verify>The top-level voice dashboard tells a coherent story before operators drill into session detail.</verify>
  <done>The shipped Control UI feels materially closer to OpenClaw’s voice surface.</done>
</task>

## Verification
Run the focused dashboard contract tests for the voice parity renderers.

## Success Criteria
- [ ] Control UI exposes a coherent top-level voice and call operator story.
- [ ] Existing voice, talk, and bounded call actions remain intact while the summary surface improves.
