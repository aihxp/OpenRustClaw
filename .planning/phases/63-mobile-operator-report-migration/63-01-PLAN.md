# Plan 63-01: Migrate the Next Mobile Operator Report into the Application Lane

## Objective

Move the mobile node operator summary report out of `crates/cli/src/commands/mobile.rs` and into `openrustclaw-app`.

## Steps

1. Add a mobile operator-report service boundary to `openrustclaw-app`.
2. Replace inline report composition in `mobile.rs` with a bounded workspace adapter that feeds that service.
3. Preserve the shipped runtime and Control UI contract through focused regression tests for the migrated mobile report.

## Verification

- `cargo test -p openrustclaw-app -- --nocapture`
- `cargo test -p openrustclaw-cli mobile_node_report_surfaces_attention_signals_through_app_lane -- --nocapture`
- `cargo test -p openrustclaw-cli dashboard_includes_mobile_operator_report_rendering -- --nocapture`
