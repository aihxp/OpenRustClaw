---
phase: 184-skill-proposal-verification-and-reuse
plan: "02"
type: execute
wave: 2
depends_on:
  - "184-01"
files_modified:
  - crates/cli/src/commands/control.rs
  - crates/cli/src/commands/skills.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/main.rs
  - crates/app/src/skill_control.rs
autonomous: true
requirements:
  - SKIL-02
  - SKIL-03
must_haves:
  truths:
    - "Operators can review, verify, install, and roll back proposals through shipped CLI, control, and MCP surfaces."
    - "Approved proposals become active only by flowing through the existing workspace skill install and compile path."
    - "Installed skills preserve proposal provenance and verification state for later inspection."
  artifacts:
    - path: "crates/cli/src/commands/control.rs"
      provides: "Workspace-scoped proposal control flow"
      contains: "list, inspect, review, verify, install, and rollback commands"
    - path: "crates/cli/src/commands/start.rs"
      provides: "Runtime control and MCP proposal surfaces"
      contains: "HTTP routes and MCP handlers for proposal lifecycle operations"
    - path: "crates/cli/src/commands/skills.rs"
      provides: "Install bridge"
      contains: "proposal materialization into the existing workspace skill mutation path"
  key_links:
    - from: "crates/cli/src/commands/control.rs"
      to: "crates/app/src/skill_proposals.rs"
      via: "typed proposal lifecycle operations"
      pattern: "proposal|verify|install"
    - from: "crates/cli/src/commands/control.rs"
      to: "crates/cli/src/commands/skills.rs"
      via: "materialize proposal and reuse install/compile path"
      pattern: "install|compile|verify"
---

<objective>
Ship the operator-facing proposal workflow and bridge approved proposals into the existing skill runtime path.

Purpose: A durable proposal queue is not useful unless operators can work it through the same CLI/control/MCP surfaces that already manage skills and learning artifacts.
Output: proposal lifecycle commands, runtime control routes, MCP tools, install bridging into the existing skill path, and provenance-rich rollback behavior.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-skill-proposal-verification-and-reuse/184-CONTEXT.md
@.planning/phases/184-skill-proposal-verification-and-reuse/184-RESEARCH.md
@.planning/phases/184-skill-proposal-verification-and-reuse/184-VALIDATION.md
@crates/app/src/skill_control.rs
@crates/cli/src/commands/control.rs
@crates/cli/src/commands/skills.rs
@crates/cli/src/commands/start.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI and control flows for proposal review, verification, and install</name>
  <files>crates/cli/src/commands/control.rs, crates/cli/src/main.rs, crates/app/src/skill_control.rs</files>
  <behavior>
    - Test 1: operators can list and inspect proposals with provenance and verification state.
    - Test 2: unapproved or unverified proposals are rejected before install.
    - Test 3: install reports include installed skill identity and proposal lineage.
  </behavior>
  <action>Extend the workspace control command set with proposal lifecycle operations. Keep handlers thin over the app-layer proposal service and return bounded typed payloads that mirror the existing learning and skill control surfaces.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli control -- --nocapture</automated>
  </verify>
  <done>Operators can drive proposal review and activation without manually copying files or editing raw state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose runtime control and MCP proposal tools and bridge install into the existing skill path</name>
  <files>crates/cli/src/commands/skills.rs, crates/cli/src/commands/start.rs</files>
  <behavior>
    - Test 1: runtime control routes can review, verify, install, and roll back proposals.
    - Test 2: MCP tools expose bounded proposal lifecycle operations.
    - Test 3: proposal install materializes the proposal into the active skill path and reuses the existing install/compile path.
  </behavior>
  <action>Add HTTP and MCP proposal handlers that call the shared control/service layer. Implement the install bridge so approved proposals become workspace skills through the current install/compile spine, with provenance persisted back onto the proposal record.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli mcp_server_control_tools -- --nocapture</automated>
    <automated>cargo test -p openrustclaw-cli skills -- --nocapture</automated>
    <automated>cargo check -p openrustclaw-cli --tests</automated>
  </verify>
  <done>Approved, verified proposals become active via the same runtime path as normal skills, with auditable lineage.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-02-01 | E | proposal install bridge | mitigate | Require approved and verified state before materializing a workspace skill |
| T-184-02-02 | I | operator inspection | mitigate | Return bounded proposal status, provenance, and verification payloads instead of raw file dumps |
| T-184-02-03 | R | rollback | mitigate | Persist installed skill linkage so rollback can deactivate or uninstall the right artifact |
</threat_model>

<verification>
- `cargo test -p openrustclaw-cli control -- --nocapture`
- `cargo test -p openrustclaw-cli skills -- --nocapture`
- `cargo test -p openrustclaw-cli mcp_server_control_tools -- --nocapture`
- `cargo check -p openrustclaw-cli --tests`
</verification>
