---
phase: 08
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .codex/get-shit-done/bin/lib/commands.cjs
  - .codex/get-shit-done/workflows/execute-phase.md
  - .planning/phases/08-verification-artifact-contract/08-VERIFICATION.md
autonomous: true
user_setup: []
requirements:
  - LIFE-01
  - LIFE-02
must_haves:
  truths:
    - Direct verification scaffolds match the structured verification schema already enforced elsewhere.
    - Workflow docs tell executors that verification artifacts are required before phase completion.
    - Phase 8 itself ends with a truthful verification artifact proving the contract changes.
  artifacts:
    - path: .codex/get-shit-done/bin/lib/commands.cjs
      purpose: Bring direct verification scaffolds into schema alignment
    - path: .codex/get-shit-done/workflows/execute-phase.md
      purpose: Document the hard verification gate on phase completion
    - path: .planning/phases/08-verification-artifact-contract/08-VERIFICATION.md
      purpose: Preserve Phase 8 verification evidence under the new contract
---

# Phase 8 Plan 03: Align verification scaffolds, workflow guidance, and phase evidence

## Objective
- **What:** Align direct verification scaffolds with the active schema and close the phase with its own verification artifact.
- **Why:** The contract is still leaky if manual scaffolding emits the wrong format or if the phase itself does not preserve verification evidence.
- **Output:** Schema-aligned verification scaffolding, updated execute-phase guidance, and a final Phase 8 verification report.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-verification-artifact-contract/08-CONTEXT.md

## Tasks

<task type="code">
  <name>Fix direct verification scaffolding</name>
  <files>.codex/get-shit-done/bin/lib/commands.cjs</files>
  <action>Make `scaffold verification` emit frontmatter and section structure compatible with the current verification template/schema.</action>
  <verify>Directly scaffolded verification files parse against the declared verification schema without manual cleanup.</verify>
  <done>Operators can create compatible verification artifacts from either scaffold path.</done>
</task>

<task type="docs">
  <name>Document the hard verification gate</name>
  <files>.codex/get-shit-done/workflows/execute-phase.md</files>
  <action>Update execute-phase guidance so it explicitly states that phase completion must stop when verification artifacts are missing or stale.</action>
  <verify>Workflow docs describe the same lifecycle rule enforced by the CLI.</verify>
  <done>Execution guidance matches the new contract.</done>
</task>

<task type="docs">
  <name>Write Phase 8 verification evidence</name>
  <files>.planning/phases/08-verification-artifact-contract/08-VERIFICATION.md</files>
  <action>Capture the Phase 8 goal, artifacts, requirements coverage, and command evidence in a structured verification report.</action>
  <verify>The phase itself leaves behind the verification artifact that v1.0 was missing.</verify>
  <done>Phase 8 can complete under the contract it introduced.</done>
</task>

## Verification
Run temp-workspace CLI probes for direct scaffolding and preserve the exact commands/evidence in Phase 8's final verification report.

## Success Criteria
- [ ] `scaffold verification` emits schema-compatible output.
- [ ] Execute-phase docs state the hard verification gate clearly.
- [ ] Phase 8 preserves its own `VERIFICATION.md` artifact with concrete evidence.
