---
phase: 03
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/cli/src/commands/memory.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/control_ui.html
  - tests/integration/src
autonomous: true
user_setup: []
requirements:
  - MEM-02
must_haves:
  truths:
    - Operators can inspect why a memory was stored from shipped CLI or control surfaces.
    - Memory inspection exposes write-basis metadata instead of only raw content.
    - The memory-control surfaces stay aligned with the underlying stored metadata.
  artifacts:
    - path: crates/cli/src/commands/memory.rs
      purpose: CLI memory inspection with write-policy metadata visibility
    - path: crates/cli/src/commands/start.rs
      purpose: Control memory APIs expose write-policy metadata cleanly
    - path: crates/cli/src/commands/control_ui.html
      purpose: Memory inspection UX surfaces write reasons and policy basis
    - path: tests/integration/src
      purpose: Regression coverage for operator-visible memory write inspection
  key_links:
    - from: crates/cli/src/commands/memory.rs
      to: crates/agent/src/memory_tools.rs
      via: stored write-basis metadata becomes operator-visible memory inspection
---

# Phase 3 Plan 02: Make memory-write decisions inspectable to operators

## Objective
- **What:** Surface memory write-basis metadata in shipped operator inspection tools.
- **Why:** Phase 3 is not trustworthy if the assistant stores memories under a policy but operators still cannot see why a write happened.
- **Output:** CLI and control memory inspection surfaces that expose stored write reasons and policy basis.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-memory-durability-and-write-policy/03-CONTEXT.md
@.planning/phases/03-memory-durability-and-write-policy/03-01-SUMMARY.md

## Tasks

<task type="code">
  <name>Expose write-policy metadata in CLI memory inspection</name>
  <files>crates/cli/src/commands/memory.rs</files>
  <action>Update memory inspection and timeline commands so operators can see the write basis, reason, and source classification for stored memories.</action>
  <verify>CLI output shows more than content alone for memories created through the assistant lane.</verify>
  <done>Operators can inspect why a memory exists without going straight to SQLite.</done>
</task>

<task type="code">
  <name>Expose write-policy metadata in control memory surfaces</name>
  <files>crates/cli/src/commands/start.rs, crates/cli/src/commands/control_ui.html</files>
  <action>Ensure control memory APIs and UI render the same write-policy metadata in a concise operator-friendly form.</action>
  <verify>The browser operator surface can identify explicit-request versus durable-fact memories quickly.</verify>
  <done>Control surfaces tell the same memory trust story as the CLI and storage layer.</done>
</task>

## Verification
Inspect memory timeline or detail surfaces in CLI and control APIs to confirm stored policy-basis metadata is visible and consistent.

## Success Criteria
- [ ] Operators can inspect why a memory was stored from shipped surfaces.
- [ ] CLI and control surfaces expose write-basis metadata consistently.
- [ ] Memory inspection becomes useful for debugging write-policy decisions.
