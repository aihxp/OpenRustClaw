---
phase: 11
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
  - tests/integration/src
requirements:
  - BROW-01
  - BROW-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operators can inspect recent browser workflow history from shipped runtime surfaces.
    - Control UI renders browser workflow history directly instead of forcing raw endpoint discovery.
    - The new surface remains backed by typed runtime data rather than frontend-only state.
  artifacts:
    - path: crates/cli/src/commands/start.rs
      purpose: Expose recent browser workflow history through a control endpoint
    - path: crates/cli/src/commands/control_ui.html
      purpose: Render recent browser workflow history in the shipped operator dashboard
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Lock the browser workflow history panel contract
    - path: tests/integration/src
      purpose: Regression coverage for the browser workflow history contract
  key_links:
    - from: crates/cli/src/commands/start.rs
      to: crates/cli/src/commands/control_ui.html
      via: `/control/browser/workflow-history`
---

# Phase 11 Plan 02: Surface browser workflow history in runtime APIs and Control UI

## Objective
- **What:** Expose and render recent browser workflow history from shipped operator surfaces.
- **Why:** Durable history only closes the parity gap if operators can inspect it from the runtime and dashboard without opening workspace files.
- **Output:** A typed control endpoint plus a Control UI browser workflow history panel and focused regression coverage.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-browser-automation-depth/11-CONTEXT.md

## Tasks

<task type="code">
  <name>Expose recent browser workflow history through the runtime</name>
  <files>crates/cli/src/commands/start.rs</files>
  <action>Add a shipped control endpoint that returns recent browser workflow history records with limit support.</action>
  <verify>Operators can fetch recent browser workflow runs through a stable runtime path.</verify>
  <done>The browser workflow ledger becomes visible from the control plane.</done>
</task>

<task type="code">
  <name>Render browser workflow history in Control UI</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Add a recent browser workflows panel wired to the new runtime endpoint and lock it with a dashboard contract test.</action>
  <verify>The dashboard loads recent browser workflows and remains truthful when no history exists yet.</verify>
  <done>Browser depth becomes operator-visible from the shipped UI.</done>
</task>

<task type="code">
  <name>Add regression coverage for browser workflow history</name>
  <files>tests/integration/src</files>
  <action>Create focused coverage proving browser workflow history persists and remains inspectable through the runtime contract.</action>
  <verify>Tests fail if recent browser workflows disappear or the contract regresses.</verify>
  <done>The browser history surface is defended by automation.</done>
</task>

## Verification
Run focused CLI and integration tests that prove the runtime and dashboard show recent browser workflow history.

## Success Criteria
- [ ] Recent browser workflow history is available from a shipped runtime endpoint.
- [ ] Control UI renders the browser workflow history panel.
- [ ] Browser workflow history contract is protected by tests.
