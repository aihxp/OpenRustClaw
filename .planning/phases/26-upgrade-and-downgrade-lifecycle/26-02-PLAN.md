# Plan 26-02: Ship The Upgrade And Downgrade Operator Loop

## Goal

Expose the transition lifecycle through the runtime API and Control UI so operators can change modes without editing files by hand.

## Must Have

- runtime exposes a transition write path for product mode
- Control UI shows recent transitions and allows upgrade or downgrade requests
- the shipped UI reflects warnings and the current transition envelope

## Expected Files

- `crates/cli/src/commands/start.rs`
- `crates/cli/src/commands/control_ui.html`
- `crates/cli/src/commands/control_ui.rs`

## Verification

- targeted panel-wiring coverage and transition-summary tests
