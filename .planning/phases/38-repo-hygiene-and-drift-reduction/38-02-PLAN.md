# Plan 38-02

## Goal

Make local sidecar Python state explicitly non-canonical repo surface.

## Steps

1. Tighten `.gitignore` for sidecar-specific generated paths.
2. Document the sidecar local-artifact boundary in `sidecar/README.md`.

## Verification

- `git check-ignore -v sidecar/.venv sidecar/.pytest_cache sidecar/src/__pycache__`
- `rg -n "non-canonical|\\.venv|__pycache__" sidecar/README.md`
