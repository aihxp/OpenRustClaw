---
phase: 182-structured-memory-artifacts-and-model-control
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/types.rs
  - crates/memory/src/policies.rs
  - crates/memory/src/model_artifacts.rs
  - crates/memory/src/lib.rs
  - crates/db/src/models.rs
  - crates/db/src/migrate.rs
  - crates/db/src/memory_store.rs
autonomous: true
requirements:
  - MODL-01
  - MODL-02
  - MODL-03
must_haves:
  truths:
    - "The runtime stores user, operator, project, and archive model artifacts as typed durable records instead of one blended profile."
    - "Model-artifact promotion is policy-gated and preserves source lineage."
    - "Only a bounded subset of active artifacts projects into active context."
  artifacts:
    - path: "crates/core/src/types.rs"
      provides: "Shared typed model-artifact contracts"
      contains: "artifact kind, status, lineage, and projection types"
    - path: "crates/db/src/memory_store.rs"
      provides: "Durable model-artifact storage and superseding promotion path"
      contains: "promotion, listing, mutation, and projection sync"
    - path: "crates/memory/src/model_artifacts.rs"
      provides: "Projection shaping"
      contains: "reserved core-memory keys and bounded projection rules"
  key_links:
    - from: "crates/db/src/memory_store.rs"
      to: "crates/memory/src/policies.rs"
      via: "promotion policy evaluation"
      pattern: "evaluate_model_artifact"
    - from: "crates/db/src/memory_store.rs"
      to: "crates/memory/src/model_artifacts.rs"
      via: "projection sync into reserved core-memory keys"
      pattern: "project|reserved"
---

<objective>
Introduce the durable typed model-artifact layer and bounded projection path that later learning work will build on.

Purpose: Phase 182 needs a Rust-owned source of truth for structured user/operator/project/archive summaries before reviewable learning or God Mode work can safely land.
Output: shared model-artifact types, a durable artifact table, policy-gated promotion with lineage, and projection synced into bounded core-memory keys.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-structured-memory-artifacts-and-model-control/182-CONTEXT.md
@.planning/phases/182-structured-memory-artifacts-and-model-control/182-RESEARCH.md
@crates/core/src/types.rs
@crates/memory/src/policies.rs
@crates/db/src/memory_store.rs
@crates/db/src/core_memory_store.rs
@crates/memory/src/context.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared model-artifact and projection contracts</name>
  <files>crates/core/src/types.rs, crates/memory/src/policies.rs, crates/memory/src/model_artifacts.rs, crates/memory/src/lib.rs</files>
  <behavior>
    - Test 1: model-artifact kind and status contracts serialize cleanly.
    - Test 2: promotion policy blocks missing-lineage or empty-summary writes.
    - Test 3: projection stays bounded to reserved typed core-memory slots.
  </behavior>
  <action>Add shared types for model artifacts, lineage, promotion requests, mutation requests, and projection snapshots. Extend `MemoryPolicies` with explicit model-artifact promotion rules. Add a dedicated `model_artifacts` helper module in `crates/memory` that maps active artifacts into reserved bounded core-memory slots without widening prompt injection.</action>
  <verify>
    <automated>cargo test -p openrustclaw-core --lib</automated>
    <automated>cargo test -p openrustclaw-memory model_artifacts -- --nocapture</automated>
  </verify>
  <done>Phase 182 has one typed contract for storage, policy, and projection instead of ad hoc JSON blobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add durable storage, promotion, and superseding projection sync</name>
  <files>crates/db/src/models.rs, crates/db/src/migrate.rs, crates/db/src/memory_store.rs</files>
  <behavior>
    - Test 1: artifacts round-trip with kind, status, lineage, and timestamps.
    - Test 2: promoting a new active artifact supersedes any previous active artifact of the same kind/namespace.
    - Test 3: projection sync updates reserved core-memory keys and removes stale keys after deactivation/removal.
  </behavior>
  <action>Add a durable `memory_model_artifacts` table and row model. Extend `SqliteMemoryStore` with list/get/promote/correct/deactivate/remove methods plus projection sync that materializes active artifacts into reserved core-memory slots. Keep projection bounded to the Phase 182 artifact kinds and preserve lineage through mutations.</action>
  <verify>
    <automated>cargo test -p openrustclaw-db memory_store -- --nocapture</automated>
  </verify>
  <done>The runtime has one Rust-owned source of truth for structured model artifacts and a live bounded projection path into active context.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01-01 | I | projection sync | mitigate | Keep projection to reserved core-memory keys only and clip summaries |
| T-182-01-02 | T/R | promotion flow | mitigate | Require explicit lineage and persist superseding history instead of overwriting raw evidence |
| T-182-01-03 | D | core-memory materialization | mitigate | Bound the projected slot count to the four typed artifact classes |
</threat_model>

<verification>
- `cargo test -p openrustclaw-core --lib`
- `cargo test -p openrustclaw-memory model_artifacts -- --nocapture`
- `cargo test -p openrustclaw-db memory_store -- --nocapture`
</verification>
