---
phase: 182-structured-memory-artifacts-and-model-control
plan: "02"
type: execute
wave: 2
depends_on:
  - "182-01"
files_modified:
  - crates/app/src/memory_views.rs
  - crates/cli/src/commands/memory.rs
  - crates/cli/src/commands/inspect.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/main.rs
autonomous: true
requirements:
  - MODL-04
must_haves:
  truths:
    - "Operators can inspect structured model artifacts through existing memory surfaces."
    - "Operators can correct, deactivate, and remove structured model artifacts without editing raw memory files directly."
    - "Mutation paths resync bounded projection so stale artifacts stop influencing active context."
  artifacts:
    - path: "crates/cli/src/commands/memory.rs"
      provides: "Operator memory-artifact commands"
      contains: "list, promote, correct, deactivate, and remove paths"
    - path: "crates/cli/src/commands/inspect.rs"
      provides: "Typed artifact inspection report"
      contains: "artifact status, lineage, and projected core-memory view"
    - path: "crates/cli/src/commands/start.rs"
      provides: "Existing MCP memory-control extension"
      contains: "artifact inspection and mutation handlers"
  key_links:
    - from: "crates/cli/src/main.rs"
      to: "crates/cli/src/commands/memory.rs"
      via: "new memory artifact subcommands"
      pattern: "MemoryAction"
    - from: "crates/cli/src/commands/start.rs"
      to: "crates/cli/src/commands/inspect.rs"
      via: "MCP inspection route uses typed artifact report"
      pattern: "model_artifact"
---

<objective>
Expose typed operator control for structured model artifacts through already-shipped OpenRustClaw surfaces.

Purpose: Phase 182 is not complete if artifacts exist but operators cannot review or repair them safely.
Output: CLI and MCP inspection plus mutation paths that preserve lineage and immediately resync bounded projection.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-structured-memory-artifacts-and-model-control/182-CONTEXT.md
@.planning/phases/182-structured-memory-artifacts-and-model-control/182-RESEARCH.md
@crates/app/src/memory_views.rs
@crates/cli/src/commands/memory.rs
@crates/cli/src/commands/inspect.rs
@crates/cli/src/commands/start.rs
@crates/cli/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add operator CLI and inspect reporting for model artifacts</name>
  <files>crates/app/src/memory_views.rs, crates/cli/src/commands/memory.rs, crates/cli/src/commands/inspect.rs, crates/cli/src/main.rs</files>
  <behavior>
    - Test 1: CLI listing returns typed artifact details and projected core-memory slots.
    - Test 2: correct/deactivate/remove flows mutate artifacts without dropping lineage.
    - Test 3: projected reserved core-memory keys update immediately after mutation.
  </behavior>
  <action>Extend the memory CLI with artifact-aware commands and add a typed inspection report that shows status, lineage, and projected core-memory state. Keep the operator interaction within existing memory/inspect surfaces and avoid raw DB editing workflows.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli memory -- --nocapture</automated>
    <automated>cargo test -p openrustclaw-cli inspect -- --nocapture</automated>
  </verify>
  <done>Operators can manage model artifacts through the standard CLI flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend MCP memory tools for artifact inspection and mutation</name>
  <files>crates/cli/src/commands/start.rs</files>
  <behavior>
    - Test 1: MCP handlers can list and inspect model artifacts.
    - Test 2: MCP mutation handlers can promote, correct, deactivate, and remove artifacts.
    - Test 3: mutations return bounded typed payloads and keep projection synchronized.
  </behavior>
  <action>Register new MCP handlers on the existing memory-control lane so remote operators and runtime tooling can inspect and manage Phase 182 model artifacts. Reuse the same typed contracts and keep responses bounded.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli mcp_server_memory_tools_persist_and_render -- --nocapture</automated>
    <automated>cargo check -p openrustclaw-cli --tests</automated>
  </verify>
  <done>Phase 182 artifact control is available through the existing MCP/control integration lane.</done>
</task>

</tasks>

<verification>
- `cargo test -p openrustclaw-cli memory -- --nocapture`
- `cargo test -p openrustclaw-cli inspect -- --nocapture`
- `cargo test -p openrustclaw-cli mcp_server_memory_tools_persist_and_render -- --nocapture`
- `cargo check -p openrustclaw-cli --tests`
</verification>
