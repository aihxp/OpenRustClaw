# Plan 45-01: Audit the Live Release Workflow Failure

## Objective

Tie the release repair work to the current failing GitHub run and specific broken jobs.

## Steps

1. Inspect the latest `release-binaries.yml` run and enumerate job outcomes.
2. Pull the failed Linux job logs and extract the exact missing dependency or environment failures.
3. Record the target-specific contract for the next repair phase.

## Verification

- `env -u GITHUB_TOKEN gh run list --repo aihxp/OpenRustClaw --workflow release-binaries.yml --limit 6 --json databaseId,headBranch,conclusion,url`
- `env -u GITHUB_TOKEN gh run view 23673584206 --repo aihxp/OpenRustClaw --json jobs,url`
- `env -u GITHUB_TOKEN gh api repos/aihxp/OpenRustClaw/actions/jobs/68972150932/logs`
- `env -u GITHUB_TOKEN gh api repos/aihxp/OpenRustClaw/actions/jobs/68972150928/logs`
