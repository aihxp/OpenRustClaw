---
phase: 191-cursor-surface-verification-and-backend-expansion
plan: "02"
type: execute
wave: 2
depends_on: [191-01]
files_modified:
  - crates/app/src/onboarding_lane_catalog.rs
  - crates/cli/src/commands/models.rs
  - crates/cli/src/commands/onboard.rs
autonomous: true
requirements:
  - CURS-01
  - CURS-03
must_haves:
  truths:
    - "Onboarding and model surfaces expose Cursor consistently with its new contract."
    - "Subscription-managed Cursor onboarding validates local signed-in readiness without pretending operator policy is already open."
    - "Cursor-discovered models are visible where operators choose their first lane and task model."
---

<objective>
Carry the new Cursor contract into onboarding and model-selection surfaces so the product stops calling Cursor detection-only while still respecting the policy boundary.
</objective>

<tasks>
- Let providerless delegated backends advertise a `subscription_managed` onboarding path.
- Show discovered Cursor model options in delegated backend model surfaces.
- Validate Cursor onboarding against local delegated-backend readiness rather than enterprise allowlists.
</tasks>

<verification>
- `cargo test -p openrustclaw-cli models -- --nocapture`
- `cargo test -p openrustclaw-cli onboard -- --nocapture`
</verification>
