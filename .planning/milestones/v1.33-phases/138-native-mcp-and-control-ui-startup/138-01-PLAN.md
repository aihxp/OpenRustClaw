# Plan 138-01

## Goal

Define the first MCP-native startup slice and the Control UI serving alignment needed for the successor startup path.

## Steps

1. Define the first implemented MCP-native startup slice explicitly.
2. Align Control UI serving to the gateway-native startup path instead of leaving it coupled to `start.rs`.
3. Verify that the milestone leaves a concrete source-level MCP and Control UI startup slice.
