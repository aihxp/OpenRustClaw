# Plan 139-01

## Goal

Define the first bounded `start.rs` startup handoff so the implementation roadmap has an explicit initial ownership transfer instead of vague bootstrap retirement language.

## Steps

1. Define which startup responsibilities leave `start.rs` first.
2. Define any bounded compatibility forwarding that survives the first handoff slice.
3. Verify that the milestone preserves a truthful path for later source-level deletion or isolation.
