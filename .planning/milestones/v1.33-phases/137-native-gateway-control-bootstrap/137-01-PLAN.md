# Plan 137-01

## Goal

Define the first gateway-native control bootstrap slice so the implementation roadmap has a concrete successor startup path for control HTTP ownership.

## Steps

1. Define the first implemented gateway-native control bootstrap slice explicitly.
2. Tie that slice to startup ownership in `openrustclaw-gateway` instead of `start.rs`.
3. Verify that the milestone leaves a concrete implementation path for the first control successor entrypoint.
