# Plan 128-01

## Goal

Define the direct verification and ownership-exit rules for repository and integration adapters so the next implementation slice can reduce command-local persistence ownership truthfully.

## Steps

1. Identify how repository-backed and gateway-backed behavior should be verified directly.
2. Define what command modules are no longer allowed to own once adapter-backed paths exist.
3. Verify that the roadmap only advances to `6/8` because the adapter-replacement slice is explicit end to end.
