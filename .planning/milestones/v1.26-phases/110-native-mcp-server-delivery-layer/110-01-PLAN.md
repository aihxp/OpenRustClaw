# Plan 110-01

## Goal

Make the native MCP delivery path concrete enough that future implementation can replace the legacy bootstrap without re-deriving transport ownership.

## Steps

1. Define MCP transport ownership in `openrustclaw-mcp`.
2. Define `McpServerPort` as the app-facing contract for tool catalog and invocation.
3. Verify that the roadmap states where temporary MCP forwarding may live without regaining ownership.
