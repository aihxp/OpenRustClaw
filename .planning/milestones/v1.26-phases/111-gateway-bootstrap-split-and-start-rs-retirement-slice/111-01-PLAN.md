# Plan 111-01

## Goal

Turn `start.rs` retirement into an explicit startup-ownership transition instead of a vague cleanup claim.

## Steps

1. Map control, websocket, webhook, and MCP startup responsibilities away from `start.rs`.
2. Name the native startup targets for gateway and MCP delivery.
3. Verify that the roadmap leaves `start.rs` with a bounded forwarding role rather than unowned future cleanup.
