# Plan 91-01

## Goal

Extract the provider-resolution and voice-session lifecycle state machine into `openrustclaw-app`.

## Steps

1. Add an app-side voice runtime lifecycle service that owns provider resolution, session start, append, respond, reconnect, pause, resume, interrupt, health, reap, and prewarm shaping.
2. Update `voice_runtime.rs` to use the lifecycle service while keeping actual synthesis, metadata probing, and session persistence in the adapter.
3. Verify the new service with app tests and a clean CLI compile.
