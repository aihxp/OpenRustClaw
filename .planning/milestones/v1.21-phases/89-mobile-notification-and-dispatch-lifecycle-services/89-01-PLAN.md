# Plan 89-01

## Goal

Extract the mobile notification, outbound message, dispatch, approval, wake, and rehydrate lifecycle rules behind `openrustclaw-app`.

## Steps

1. Add an app-side mobile runtime control service with lifecycle record and command-transition helpers.
2. Update `mobile.rs` to use the new service while keeping persistence and command execution as adapter concerns.
3. Verify the extracted lane with focused app-unit coverage and a clean CLI compile.
