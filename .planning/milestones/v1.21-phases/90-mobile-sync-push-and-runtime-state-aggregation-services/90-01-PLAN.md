# Plan 90-01

## Goal

Extract the push, sync, heartbeat, runtime activity, node summary, and mobile metrics aggregation helpers into `openrustclaw-app`.

## Steps

1. Add an app-side mobile runtime status service with runtime refresh, push or sync shaping, and rollup helpers.
2. Update `mobile.rs` to call the new service for status, summary, and aggregation responses.
3. Verify the new service with app tests and a clean CLI compile.
