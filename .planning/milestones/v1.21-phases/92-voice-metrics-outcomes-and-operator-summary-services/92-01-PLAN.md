# Plan 92-01

## Goal

Extract transcript, artifact, event, metrics, and outcome composition for the voice runtime into `openrustclaw-app`.

## Steps

1. Add an app-side voice runtime reporting service for transcript, artifact, event, metrics, and outcome composition.
2. Update `voice_runtime.rs` to gather file metadata and pass shaped session state into the reporting service.
3. Verify the extracted reporting lane with app tests and a clean CLI compile.
