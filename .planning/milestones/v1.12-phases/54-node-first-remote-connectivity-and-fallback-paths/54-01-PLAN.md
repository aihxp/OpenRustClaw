# Plan 54-01: Persist the Node-First Remote Connectivity Profile

## Objective

Turn the remote-connectivity contract into a real onboarding and setup-state surface instead of leaving it as docs-only guidance.

## Steps

1. Extend onboarding and setup state with a serializable remote-connectivity profile.
2. Capture the remote-connectivity choice during gateway setup and write a matching bootstrap outcome.
3. Surface the persisted profile through the setup handoff report so later operator surfaces can build on it.

## Verification

- `cargo test -p openrustclaw-cli onboard -- --nocapture`
- `cargo test -p openrustclaw-cli setup_handoff_summary -- --nocapture`
