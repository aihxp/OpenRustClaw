---
wave: 1
---

# Plan 84-01: Extract the Runtime, Voice, Talk, and Mobile Status Route Family

## Objective

Move runtime, voice, talk, and mobile status orchestration out of `start.rs` and behind one shared application-layer status service.

## Steps

1. Add a runtime, voice, talk, and mobile status service to `openrustclaw-app` for report orchestration, limit normalization, and stable result shaping.
2. Replace the inline status handlers in `start.rs` with adapters into that service.
3. Verify the shipped runtime, voice, talk, and mobile status route family still preserves the current contract.

## Verification

- `cargo test -p openrustclaw-app operator_status_control -- --nocapture`
- `cargo test -p openrustclaw-cli runtime_voice_talk_and_mobile_status_route_family_uses_service_lane -- --nocapture`
