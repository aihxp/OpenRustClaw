---
phase: 191-cursor-surface-verification-and-backend-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/app/src/agent_backend_catalog.rs
  - crates/app/src/agent_backend_control.rs
  - crates/app/src/runtime_provider_switch.rs
  - crates/cli/src/commands/runtime.rs
autonomous: true
requirements:
  - CURS-01
  - CURS-02
must_haves:
  truths:
    - "Cursor is classified from current executable evidence instead of stale milestone assumptions."
    - "Cursor reuses the delegated backend contract when its local CLI surface is execution-capable."
    - "Runtime execution support stays bounded and policy-aware rather than introducing a side path."
---

<objective>
Promote Cursor into the delegated backend contract by proving the local `cursor agent` surface is real, typed, and routeable through the same local CLI wrapper path used by the other delegated backends.
</objective>

<tasks>
- Update agent-backend discovery to probe `cursor agent status` and `cursor agent models`, parse Cursor auth plus model evidence, and classify Cursor as a delegated CLI candidate instead of integration-only.
- Add Cursor execution support to the delegated CLI runtime provider and allow runtime provider switching to recognize `cursor`.
- Keep the model choice vendor-managed by default while allowing later flows to reuse a setup-selected Cursor model when one is present.
</tasks>

<verification>
- `cargo fmt --all`
- `cargo test -p openrustclaw-app agent_backend_catalog -- --nocapture`
- `cargo test -p openrustclaw-app runtime_provider_switch -- --nocapture`
</verification>
