# Plan 120-01

## Goal

Define the UI-adjacent alignment needed to keep the second CLI slice end to end instead of splitting native ownership by transport.

## Steps

1. Align UI-adjacent operator surfaces to native entrypoints across CLI and gateway delivery.
2. Preserve compatibility for shipped operator surfaces while reducing conceptual legacy ownership.
3. Verify that the roadmap only advances to `4/8` once the second CLI slice is explicit end to end.
