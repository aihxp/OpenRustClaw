# Plan 119-01

## Goal

Define the dependency-removal rules needed for native CLI modules to own their flows directly instead of recreating legacy internal routing.

## Steps

1. State which command-to-command dependencies must be removed or avoided.
2. Define native delivery modules as independent entrypoint families over app ports.
3. Verify that compatibility remains possible without reviving legacy internal routing as architecture.
