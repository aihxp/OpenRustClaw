# Plan 117-01

## Goal

Define the native CLI ownership path for the remaining large operator families so those flows can leave the legacy command tree without rediscovering delivery boundaries.

## Steps

1. Map the remaining large operator families to their app-port-backed native delivery modules.
2. Make those families explicit in the roadmap instead of treating them as generic future cleanup.
3. Verify that the milestone keeps the implementation path incremental rather than turning into a vague rewrite.
