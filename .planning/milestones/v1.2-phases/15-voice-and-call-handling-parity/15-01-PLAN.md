---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/inspect.rs
  - crates/cli/src/commands/start.rs
  - tests/integration/src/voice_operator_report_test.rs
requirements:
  - VOIC-01
  - VOIC-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operators get one typed voice and call summary instead of stitching voice, talk, and bounded call state together manually.
    - Attention-worthy voice or call conditions are surfaced from durable receipts instead of hidden in raw payloads.
  artifacts:
    - path: crates/cli/src/commands/inspect.rs
      purpose: Aggregate the typed voice operator report
    - path: crates/cli/src/commands/start.rs
      purpose: Expose the report through a shipped runtime route
    - path: tests/integration/src/voice_operator_report_test.rs
      purpose: Prove the report surfaces real attention signals
---

# Phase 15 Plan 01: Add a typed voice operator report

## Objective
- **What:** Aggregate the shipped voice, talk, and bounded call data into one typed operator report and route.
- **Why:** Voice parity is currently limited more by fragmented operator visibility than by missing raw data.
- **Output:** A typed runtime summary with focused automated coverage.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-voice-and-call-handling-parity/15-CONTEXT.md

## Tasks

<task type="code">
  <name>Aggregate a voice operator report</name>
  <files>crates/cli/src/commands/inspect.rs, crates/cli/src/commands/start.rs</files>
  <action>Combine voice runtime, talk runtime, and bounded voice-call summaries into one typed report with readiness, attention signals, and recent activity.</action>
  <verify>Operators can inspect a single runtime report instead of combining several separate voice routes manually.</verify>
  <done>The runtime exposes a coherent voice operator summary route.</done>
</task>

<task type="test">
  <name>Lock attention-signal behavior</name>
  <files>tests/integration/src/voice_operator_report_test.rs</files>
  <action>Create focused integration coverage proving stale voice sessions, talk errors, and stale bounded calls surface in the report.</action>
  <verify>The report fails loudly if the operator summary stops surfacing attention-worthy voice conditions.</verify>
  <done>The report has focused evidence tied to the parity contract.</done>
</task>

## Verification
Run focused CLI and integration tests for the new report.

## Success Criteria
- [ ] A typed voice operator report exists and is routed through the shipped control API.
- [ ] The report surfaces readiness, attention, and recent activity across voice, talk, and bounded calls.
