---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/browser.rs
requirements:
  - BROW-01
  - BROW-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Rich browser workflow runs persist a durable operator-readable history record instead of disappearing into individual artifact files.
    - Each recent history record captures backend, session context, final destination, step count, outcome, and artifact path.
    - Browser workflow history remains grounded in the current policy-bounded browser lane rather than bypassing backend audit and policy.
  artifacts:
    - path: crates/cli/src/commands/browser.rs
      purpose: Persist stable browser workflow history records and expose read helpers
  key_links:
    - from: browser sequence execution
      to: durable browser workflow history
      via: workspace-local persisted records under the shipped browser control plane
---

# Phase 11 Plan 01: Add a durable browser workflow history ledger

## Objective
- **What:** Persist durable browser workflow history records for richer browser runs.
- **Why:** Phase 11 cannot claim meaningful browser depth while operators still have to infer recent workflow runs from raw artifact files and scattered action output.
- **Output:** A stable browser workflow history record model with read and write helpers in the Rust browser command layer.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-browser-automation-depth/11-CONTEXT.md

## Tasks

<task type="code">
  <name>Define and persist browser workflow history records</name>
  <files>crates/cli/src/commands/browser.rs</files>
  <action>Add a workspace-local browser workflow history ledger plus helper APIs for appending and listing recent workflow runs.</action>
  <verify>Recent workflow records can be written and read back with stable ordering and key inspection fields.</verify>
  <done>Browser workflow depth has a durable evidence backbone instead of only raw artifacts.</done>
</task>

<task type="code">
  <name>Capture recent workflow outcomes from sequence execution</name>
  <files>crates/cli/src/commands/browser.rs</files>
  <action>Extend browser sequence execution so successful richer workflows append truthful history entries tied to backend, session, final URL, and artifact output.</action>
  <verify>Sequence runs automatically leave behind recent-history evidence without changing the existing backend trust boundary.</verify>
  <done>Multi-step browser workflows become durably inspectable after execution.</done>
</task>

## Verification
Run focused Rust tests covering history append and read behavior for the browser workflow ledger.

## Success Criteria
- [ ] Browser workflow runs persist a durable recent-history record.
- [ ] History entries capture backend, session, step, and artifact context.
- [ ] The history layer preserves the existing policy-bounded browser lane.
