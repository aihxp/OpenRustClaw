---
phase: 12
plan: 02
type: execute
wave: 1
depends_on:
  - 12-01
files_modified:
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
requirements:
  - SUPR-01
  - SUPR-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operators can inspect richer delegated-run supervision from shipped runtime endpoints and Control UI.
    - The dashboard renders worker outcomes, delegated tasks, and live-run supervision state as structured views instead of raw dumps.
    - The new UI remains backed by typed runtime contracts instead of frontend-only derivation.
  artifacts:
    - path: crates/cli/src/commands/start.rs
      purpose: Expose typed receipt and active-run supervision endpoints
    - path: crates/cli/src/commands/control_ui.html
      purpose: Render richer supervision tables in the shipped dashboard
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Lock the new supervision panels with dashboard contract tests
  key_links:
    - from: crates/cli/src/commands/start.rs
      to: crates/cli/src/commands/control_ui.html
      via: orchestration supervision endpoints used directly by Control UI
---

# Phase 12 Plan 02: Surface richer supervision in runtime APIs and Control UI

## Objective
- **What:** Wire the richer supervision summaries into shipped runtime endpoints and Control UI.
- **Why:** Typed summaries only close the parity gap if operators can inspect them from the same runtime and dashboard they already use.
- **Output:** Updated orchestration runtime handlers plus structured supervision rendering in Control UI.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-multi-agent-supervision-parity/12-CONTEXT.md

## Tasks

<task type="code">
  <name>Expose richer supervision through shipped orchestration routes</name>
  <files>crates/cli/src/commands/start.rs</files>
  <action>Update runtime route handlers to return the richer receipt and active-run supervision reports.</action>
  <verify>Operators can fetch richer supervision state without direct file access.</verify>
  <done>The runtime exposes deeper supervision parity through stable endpoints.</done>
</task>

<task type="code">
  <name>Render worker and active-run supervision in Control UI</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Add operator-readable tables or summaries for delegated tasks, worker outcomes, active-run flags, and recent live events.</action>
  <verify>The dashboard shows richer orchestration supervision without requiring raw JSON inspection for routine understanding.</verify>
  <done>Control UI becomes a materially better supervision surface.</done>
</task>

## Verification
Run focused dashboard contract tests plus orchestration tests after the new runtime/UI surfaces are wired.

## Success Criteria
- [ ] Runtime endpoints expose richer supervision summaries.
- [ ] Control UI renders worker outcomes and live-run supervision clearly.
- [ ] The richer UI is backed by typed runtime data.
