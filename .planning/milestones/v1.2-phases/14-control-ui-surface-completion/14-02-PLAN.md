---
phase: 14
plan: 02
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
requirements:
  - CTRL-01
  - CTRL-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - The upgraded Control UI flow spans multiple deeper surfaces coherently instead of stopping at isolated panels.
    - New renderers reuse current APIs and helper patterns instead of introducing one-off frontend logic.
  artifacts:
    - path: crates/cli/src/commands/control_ui.html
      purpose: Extend typed rendering to a second batch of remaining raw-detail panes
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Preserve renderer contract coverage for the second batch
---

# Phase 14 Plan 02: Extend typed rendering across the remaining priority panes

## Objective
- **What:** Finish the next batch of high-priority dashboard detail renderers.
- **Why:** One good renderer is not enough if operators still hit raw dumps on the adjacent parity-critical flows.
- **Output:** A second batch of typed renderers that makes the dashboard feel more consistently production-ready.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-control-ui-surface-completion/14-CONTEXT.md

## Tasks

<task type="code">
  <name>Extend typed rendering to the next priority panes</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Finish the next group of raw-detail pane upgrades so the dashboard feels consistently typed across the deeper shipped surfaces.</action>
  <verify>Operators no longer need to drop back to raw detail for the adjacent parity-critical flows targeted in this batch.</verify>
  <done>The Control UI becomes materially more coherent across the deeper shipped surfaces.</done>
</task>

## Verification
Run the dashboard contract suite covering the newly upgraded panes.

## Success Criteria
- [ ] A second batch of remaining raw panes are upgraded.
- [ ] The upgraded dashboard feels more consistent across the deeper surfaces.
