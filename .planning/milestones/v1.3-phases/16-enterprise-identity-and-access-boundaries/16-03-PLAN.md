---
phase: 16
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
  - README.md
  - docs/src/deployment/production.md
autonomous: true
user_setup: []
requirements:
  - ENTE-01
  - ENTE-02
must_haves:
  truths:
    - Operators can inspect the new enterprise identity and scope boundary from shipped surfaces rather than raw files alone.
    - Documentation explains the new boundary as enterprise access foundations, not full enterprise IAM.
    - Phase verification covers registry, enforcement, and operator-facing reporting together.
  artifacts:
    - path: crates/cli/src/commands/control_ui.html
      purpose: Enterprise access summary and operator table in Control UI
    - path: README.md
      purpose: Shipped operator documentation for the new enterprise access surface
---

# Phase 16 Plan 03: Surface the enterprise access boundary in Control UI and docs

## Objective
- **What:** Make the new identity and access boundary operator-usable and verifiable.
- **Why:** Enterprise access is not credible if it only exists as hidden files and middleware.
- **Output:** A Control UI enterprise access surface, aligned operator docs, and final verification evidence for the phase.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-enterprise-identity-and-access-boundaries/16-CONTEXT.md
@.planning/phases/16-enterprise-identity-and-access-boundaries/16-01-PLAN.md
@.planning/phases/16-enterprise-identity-and-access-boundaries/16-02-PLAN.md

## Tasks

<task type="code">
  <name>Render enterprise access summary in Control UI</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Add a dedicated enterprise access panel that shows organization identity, operator counts, role coverage, and protected-route scope mapping.</action>
  <verify>Control UI fetches the typed summary and renders both headline status and operator or scope details.</verify>
  <done>Operators can inspect the access boundary without raw file inspection.</done>
</task>

<task type="docs">
  <name>Document the new enterprise access contract</name>
  <files>README.md, docs/src/deployment/production.md</files>
  <action>Explain enterprise access bootstrap, operator headers, and the narrow initial protected-route contract honestly.</action>
  <verify>Docs describe the shipped boundary as access foundations and point operators to the new runtime surface.</verify>
  <done>The milestone records a truthful operator story for enterprise access.</done>
</task>

## Verification
Run focused UI and runtime tests, then write the phase verification with passing status and any explicit residual debt.

## Success Criteria
- [ ] Control UI shows the enterprise access boundary through a dedicated operator-facing surface.
- [ ] Docs explain bootstrap and scoped operator access honestly.
- [ ] Phase verification closes with registry, enforcement, and operator reporting evidence.
