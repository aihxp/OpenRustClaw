# Plan 18-02: Expose Lifecycle Controls And Reports

## Goal

Expose escalation, rollback, and intervention evidence through the shipped runtime control routes and supervision payloads.

## Must Have

- new control routes for supervised escalation and rollback
- active supervision payloads include lifecycle state and decision records
- operator-visible reports stay grounded in typed runtime data

## Expected Files

- `crates/cli/src/commands/orchestrate.rs`
- `crates/cli/src/commands/start.rs`
- `crates/cli/src/commands/control_ui.html`
- `crates/cli/src/commands/control_ui.rs`

## Verification

- route and rendering tests for lifecycle/report surfaces
