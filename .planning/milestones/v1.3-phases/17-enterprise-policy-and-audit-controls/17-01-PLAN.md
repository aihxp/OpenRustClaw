---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/enterprise_policy.rs
  - crates/cli/src/commands/control.rs
  - crates/cli/src/commands/inspect.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/mod.rs
autonomous: true
user_setup: []
requirements:
  - ENTE-03
  - ENTE-04
must_haves:
  truths:
    - Enterprise-sensitive policy is inspectable from one coherent runtime surface instead of scattered raw endpoints.
    - Operators can update selected access, autonomy-approval, browser, mobile, and audit-export controls from one enterprise policy route.
    - Underlying config files remain the durable source of truth, not an opaque new service.
  artifacts:
    - path: crates/cli/src/commands/enterprise_policy.rs
      purpose: Unified enterprise policy snapshot and apply helpers
    - path: crates/cli/src/commands/start.rs
      purpose: `/control/enterprise/policy` runtime routes
---

# Phase 17 Plan 01: Add a unified enterprise policy surface

## Objective
- **What:** Build a typed enterprise policy report and apply route.
- **Why:** Operators still have to stitch access, autonomy approval, browser policy, and mobile approval defaults together manually.
- **Output:** A coherent `/control/enterprise/policy` surface that reads and updates the first enterprise policy slice honestly.

## Tasks

<task type="code">
  <name>Create enterprise policy module</name>
  <files>crates/cli/src/commands/enterprise_policy.rs, crates/cli/src/commands/mod.rs</files>
  <action>Add a file-backed enterprise policy manifest for mobile approval defaults and audit export settings, plus helpers that aggregate access, autonomy, and browser policy state.</action>
  <verify>The policy report includes access enforcement, approval policy, browser backend policy, mobile approval defaults, and audit export settings.</verify>
  <done>Enterprise policy is visible from one typed report.</done>
</task>

<task type="code">
  <name>Expose policy inspection and update routes</name>
  <files>crates/cli/src/commands/control.rs, crates/cli/src/commands/start.rs, crates/cli/src/commands/inspect.rs</files>
  <action>Add runtime routes that read and update the unified enterprise policy contract by writing the appropriate durable config files.</action>
  <verify>Operators can fetch and update enterprise policy from one route without manually editing multiple files.</verify>
  <done>Phase 17 has a real policy surface, not just read-only summaries.</done>
</task>

## Verification
Run focused CLI tests around policy report shape and policy write behavior.
