# Plan 76-01: Extract the Greenfield Progress Surface and Runtime Maintenance Summary Route

## Objective

Ship one truthful runtime-maintenance summary surface that reports the canonical greenfield conversion score and remaining ranked queue while moving that bounded maintenance summary behind `openrustclaw-app`.

## Steps

1. Update the canonical greenfield inventory to reflect the newly migrated seams from Phases 74 and 75.
2. Add a runtime-maintenance control service in `openrustclaw-app` and expose a shared greenfield progress summary helper.
3. Wire `/control/runtime/maintenance` through the new service and verify the shipped route reports the updated percentage and remaining queue.

## Verification

- `cargo test -p openrustclaw-app greenfield_progress -- --nocapture`
- `cargo test -p openrustclaw-app runtime_maintenance_control -- --nocapture`
- `cargo test -p openrustclaw-cli greenfield_progress_summary_reports_current_inventory_score -- --nocapture`
- `cargo test -p openrustclaw-cli runtime_maintenance_route_family_uses_service_lane -- --nocapture`
