# Plan 73-01: Define the Seam Inventory and Percentage Baseline

## Objective

Create the canonical ranked seam inventory for the greenfield conversion and back it with a reusable application-layer progress report.

## Steps

1. Add a ranked seam inventory report to `openrustclaw-app`.
2. Record the same inventory in `.planning/codebase/GREENFIELD-INVENTORY.md`.
3. Verify the report derives the canonical `12/18` and `66%` baseline.

## Verification

- `cargo test -p openrustclaw-app greenfield_progress -- --nocapture`
- manual check that `.planning/codebase/GREENFIELD-INVENTORY.md` matches the same ranks and totals
