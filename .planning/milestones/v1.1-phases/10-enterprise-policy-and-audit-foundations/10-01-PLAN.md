---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/inspect.rs
  - crates/cli/src/commands/browser.rs
  - crates/cli/src/commands/mobile.rs
  - crates/cli/src/commands/control.rs
autonomous: true
user_setup: []
requirements:
  - ENTF-01
  - ENTF-02
must_haves:
  truths:
    - Enterprise-sensitive assistant actions are described through an explicit approval or policy contract rather than implicit trust.
    - Operators can inspect durable audit evidence for recent approval-sensitive actions from a shipped runtime surface.
    - The summary reuses existing runtime, browser, mobile, and tool-execution evidence instead of inventing a disconnected enterprise ledger.
  artifacts:
    - path: crates/cli/src/commands/start.rs
      purpose: Typed enterprise foundations control endpoint
    - path: crates/cli/src/commands/inspect.rs
      purpose: Shared enterprise audit-summary logic over durable records
---

# Phase 10 Plan 01: Aggregate the enterprise approval and audit baseline

## Objective
- **What:** Build a typed enterprise foundations summary from current approval-policy and audit surfaces.
- **Why:** The repo already has durable approval and audit evidence, but operators still have to assemble the baseline manually.
- **Output:** A shipped runtime/control endpoint that reports the narrow enterprise baseline honestly.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-enterprise-policy-and-audit-foundations/10-CONTEXT.md

## Tasks

<task type="code">
  <name>Summarize approval boundary inputs</name>
  <files>crates/cli/src/commands/start.rs, crates/cli/src/commands/control.rs, crates/cli/src/commands/browser.rs</files>
  <action>Collect runtime autonomy approval policy and browser external backend policy into one typed enterprise foundations report.</action>
  <verify>The report states which side effects require approval and which browser backends are allowed.</verify>
  <done>The summary exposes the current policy contract directly.</done>
</task>

<task type="code">
  <name>Summarize durable enterprise audit evidence</name>
  <files>crates/cli/src/commands/inspect.rs, crates/cli/src/commands/mobile.rs, crates/cli/src/commands/start.rs</files>
  <action>Pull recent mobile approval timeline entries, browser backend audit entries, and approval-sensitive tool execution records into the same report.</action>
  <verify>Operators can inspect recent approval, rejection, policy-enforced backend use, and side-effect execution evidence from one endpoint.</verify>
  <done>The enterprise summary includes durable audit evidence, not just config state.</done>
</task>

## Verification
Run focused CLI tests around the new enterprise summary and inspect the serialized payload shape.

## Success Criteria
- [ ] The enterprise summary exposes runtime approval policy and browser backend policy together.
- [ ] The enterprise summary includes recent durable audit evidence for approval-sensitive actions.
- [ ] The implementation uses shipped records instead of a new ad-hoc store.
