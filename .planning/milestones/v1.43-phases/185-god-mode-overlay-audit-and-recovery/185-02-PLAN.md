---
phase: 185-god-mode-overlay-audit-and-recovery
plan: "02"
type: execute
wave: 2
depends_on:
  - "185-01"
files_modified:
  - crates/core/src/types.rs
  - crates/db/src/models.rs
  - crates/db/src/migrate.rs
  - crates/db/src/learning_store.rs
  - crates/db/src/skill_proposal_store.rs
  - crates/app/src/learning_review.rs
  - crates/app/src/skill_proposals.rs
  - crates/cli/src/commands/control.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/main.rs
autonomous: true
requirements:
  - GOD-03
  - GOD-04
must_haves:
  truths:
    - "God Mode-derived learning candidates and skill proposals preserve durable provenance and quarantine metadata."
    - "Operators can quarantine learned artifacts through shipped control surfaces."
    - "Quarantine contains active lessons or installed skills through the existing rollback and deactivate seams."
  artifacts:
    - path: "crates/core/src/types.rs"
      provides: "Shared God Mode artifact provenance and quarantine contracts"
      contains: "god_mode_origin and quarantine metadata for learned artifacts"
    - path: "crates/db/src/learning_store.rs"
      provides: "Durable learning-candidate provenance and quarantine persistence"
      contains: "queue, quarantine, and rollback-ready history"
    - path: "crates/db/src/skill_proposal_store.rs"
      provides: "Durable proposal provenance and quarantine persistence"
      contains: "queue, quarantine, and install-containment history"
    - path: "crates/cli/src/commands/control.rs"
      provides: "Operator quarantine controls"
      contains: "CLI lifecycle actions and reporting for learned artifacts"
  key_links:
    - from: "crates/app/src/learning_review.rs"
      to: "crates/db/src/learning_store.rs"
      via: "God Mode candidate provenance and quarantine"
      pattern: "god_mode|quarantine|candidate"
    - from: "crates/app/src/skill_proposals.rs"
      to: "crates/db/src/skill_proposal_store.rs"
      via: "proposal provenance inheritance and quarantine"
      pattern: "god_mode|quarantine|proposal"
    - from: "crates/cli/src/commands/start.rs"
      to: "crates/cli/src/commands/control.rs"
      via: "HTTP and MCP quarantine handlers"
      pattern: "quarantine"
---

<objective>
Thread God Mode provenance and quarantine through learned artifacts and expose the containment controls through shipped operator surfaces.

Purpose: God Mode is not safely auditable unless the artifacts it produces stay visibly marked and can be contained later without erasing history.
Output: shared artifact metadata, durable store support, app-layer quarantine behavior, CLI and runtime control handlers, and end-to-end verification.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-god-mode-overlay-audit-and-recovery/185-CONTEXT.md
@.planning/phases/185-god-mode-overlay-audit-and-recovery/185-RESEARCH.md
@.planning/phases/185-god-mode-overlay-audit-and-recovery/185-VALIDATION.md
@crates/app/src/learning_review.rs
@crates/app/src/skill_proposals.rs
@crates/db/src/learning_store.rs
@crates/db/src/skill_proposal_store.rs
@crates/cli/src/commands/control.rs
@crates/cli/src/commands/start.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist God Mode artifact provenance and quarantine metadata</name>
  <files>crates/core/src/types.rs, crates/db/src/models.rs, crates/db/src/migrate.rs, crates/db/src/learning_store.rs, crates/db/src/skill_proposal_store.rs</files>
  <behavior>
    - Test 1: learning candidates round-trip God Mode provenance and quarantine state.
    - Test 2: skill proposals round-trip God Mode provenance and quarantine state.
    - Test 3: quarantine actions append durable history without deleting artifacts.
  </behavior>
  <action>Add shared fields plus store and migration support for God Mode origin and quarantine metadata on learning candidates and skill proposals. Keep quarantine durable and audit-friendly.</action>
  <verify>
    <automated>cargo test -p openrustclaw-db learning_store -- --nocapture</automated>
    <automated>cargo test -p openrustclaw-db skill_proposal_store -- --nocapture</automated>
  </verify>
  <done>Learned artifacts now carry durable God Mode provenance and quarantine state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add quarantine-aware services and expose shipped operator controls</name>
  <files>crates/app/src/learning_review.rs, crates/app/src/skill_proposals.rs, crates/cli/src/commands/control.rs, crates/cli/src/commands/start.rs, crates/cli/src/main.rs</files>
  <behavior>
    - Test 1: quarantining a promoted learning candidate deactivates the live lesson and marks the artifact.
    - Test 2: quarantining an installed skill proposal rolls back the installed skill and marks the artifact.
    - Test 3: CLI, HTTP control, and MCP handlers surface God Mode labels and quarantine state.
  </behavior>
  <action>Extend the app-layer learning and proposal services with quarantine behavior that reuses existing deactivation and rollback seams. Then expose those actions through control CLI commands, control routes, and MCP handlers so operators can contain God Mode-derived artifacts using shipped surfaces.</action>
  <verify>
    <automated>cargo test -p openrustclaw-app learning_review -- --nocapture</automated>
    <automated>cargo test -p openrustclaw-app skill_proposals -- --nocapture</automated>
    <automated>cargo test -p openrustclaw-cli control -- --nocapture</automated>
    <automated>cargo test -p openrustclaw-cli mcp_server_control_tools -- --nocapture</automated>
    <automated>cargo check -p openrustclaw-cli --tests</automated>
  </verify>
  <done>Operators can inspect and quarantine God Mode-derived artifacts without editing raw state or leaving the shipped control surfaces.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-02-01 | I | learned artifact provenance | mitigate | Persist explicit God Mode origin fields on candidates and proposals |
| T-185-02-02 | E | active learned artifacts | mitigate | Quarantine reuses lesson deactivation and proposal rollback for containment |
| T-185-02-03 | R | audit history | mitigate | Preserve quarantine metadata and append durable history instead of deleting rows |
</threat_model>

<verification>
- `cargo test -p openrustclaw-db learning_store -- --nocapture`
- `cargo test -p openrustclaw-db skill_proposal_store -- --nocapture`
- `cargo test -p openrustclaw-app learning_review -- --nocapture`
- `cargo test -p openrustclaw-app skill_proposals -- --nocapture`
- `cargo test -p openrustclaw-cli control -- --nocapture`
- `cargo test -p openrustclaw-cli mcp_server_control_tools -- --nocapture`
- `cargo check -p openrustclaw-cli --tests`
</verification>
