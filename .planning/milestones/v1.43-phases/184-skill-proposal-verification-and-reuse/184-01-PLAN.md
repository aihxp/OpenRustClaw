---
phase: 184-skill-proposal-verification-and-reuse
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/types.rs
  - crates/db/src/models.rs
  - crates/db/src/migrate.rs
  - crates/db/src/lib.rs
  - crates/db/src/skill_proposal_store.rs
  - crates/app/src/skill_proposals.rs
  - crates/app/src/lib.rs
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
must_haves:
  truths:
    - "Skill proposals exist as durable typed records with provenance, explicit review state, and inactive artifact files."
    - "Proposal verification runs through one app-layer service instead of ad hoc CLI file mutation."
    - "Proposal artifacts remain human-readable and diffable outside the active workspace skill root."
  artifacts:
    - path: "crates/core/src/types.rs"
      provides: "Shared skill-proposal contracts"
      contains: "proposal status, provenance, verification, install, and rollback types"
    - path: "crates/db/src/skill_proposal_store.rs"
      provides: "Durable proposal queue and lifecycle storage"
      contains: "proposal CRUD, history, verification reports, and status transitions"
    - path: "crates/app/src/skill_proposals.rs"
      provides: "Review and verification policy seam"
      contains: "proposal queueing, artifact materialization, approval, verification, and rollback logic"
  key_links:
    - from: "crates/app/src/skill_proposals.rs"
      to: "crates/db/src/skill_proposal_store.rs"
      via: "proposal lifecycle persistence"
      pattern: "proposal|review|verify"
    - from: "crates/app/src/skill_proposals.rs"
      to: "crates/app/src/learning_review.rs"
      via: "source provenance from reviewed learning artifacts"
      pattern: "candidate|lesson|provenance"
---

<objective>
Introduce the durable proposal-review layer that sits between reviewed learning output and live skills.

Purpose: Phase 184 cannot be trustworthy if generated skill content drops straight into `skills/` without durable review state, provenance, and compile-aware verification.
Output: shared proposal types, SQLite-backed proposal/history storage, inactive proposal artifacts on disk, and an app service that enforces proposal lifecycle and verification rules.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-skill-proposal-verification-and-reuse/184-CONTEXT.md
@.planning/phases/184-skill-proposal-verification-and-reuse/184-RESEARCH.md
@crates/app/src/learning_review.rs
@crates/app/src/skill_registry_mutation.rs
@crates/cli/src/commands/skills.rs
@crates/db/src/learning_store.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared skill-proposal, provenance, and verification contracts</name>
  <files>crates/core/src/types.rs, crates/app/src/skill_proposals.rs, crates/app/src/lib.rs</files>
  <behavior>
    - Test 1: skill-proposal and status types serialize cleanly.
    - Test 2: unapproved proposals cannot verify or install past lifecycle gates.
    - Test 3: verification reports preserve bounded compile status and artifact metadata.
  </behavior>
  <action>Add shared types for proposal identity, provenance refs, artifact paths, review state, verification reports, install requests, and rollback results. Create a dedicated app-layer proposal service that owns queue, review, verification, and rollback rules while keeping proposals inactive by default.</action>
  <verify>
    <automated>cargo test -p openrustclaw-core --lib</automated>
    <automated>cargo test -p openrustclaw-app skill_proposals -- --nocapture</automated>
  </verify>
  <done>Phase 184 has one typed proposal contract instead of ad hoc JSON and direct `skills/` writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add durable proposal storage, history, and inactive artifact materialization</name>
  <files>crates/db/src/models.rs, crates/db/src/migrate.rs, crates/db/src/lib.rs, crates/db/src/skill_proposal_store.rs</files>
  <behavior>
    - Test 1: proposals round-trip with provenance, review state, verification metadata, and timestamps.
    - Test 2: proposal artifact files materialize outside the active `skills/` root and stay readable.
    - Test 3: verify, supersede, and rollback transitions persist durable history.
  </behavior>
  <action>Add SQLite-backed proposal and proposal-history tables plus a `skill_proposal_store` module that lists, fetches, transitions, and records verification/install outcomes. Persist the inactive proposal artifact path and any compile-summary metadata needed for review.</action>
  <verify>
    <automated>cargo test -p openrustclaw-db skill_proposal_store -- --nocapture</automated>
  </verify>
  <done>The runtime has a durable proposal-review source of truth with inactive, inspectable proposal artifacts.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-01-01 | E | proposal artifact path | mitigate | Store proposal files outside `skills/` until explicit install |
| T-184-01-02 | T/R | proposal provenance | mitigate | Persist source learning candidate and lesson refs plus proposal history |
| T-184-01-03 | D | verification metadata | mitigate | Keep verification summaries bounded and derived from existing compile outcomes |
</threat_model>

<verification>
- `cargo test -p openrustclaw-core --lib`
- `cargo test -p openrustclaw-db skill_proposal_store -- --nocapture`
- `cargo test -p openrustclaw-app skill_proposals -- --nocapture`
</verification>
