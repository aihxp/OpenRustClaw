---
phase: 183-learning-candidate-review-and-lesson-promotion
plan: "02"
type: execute
wave: 2
depends_on:
  - "183-01"
files_modified:
  - crates/cli/src/commands/orchestrate.rs
  - crates/cli/src/commands/control.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/main.rs
autonomous: true
requirements:
  - LEAR-02
  - LEAR-03
  - LEAR-04
must_haves:
  truths:
    - "Operators can review and mutate learning candidates through existing control surfaces."
    - "Reflection-candidate promotion no longer writes a live lesson directly."
    - "Approved candidates promote into the existing bounded lesson lane and support rollback."
  artifacts:
    - path: "crates/cli/src/commands/orchestrate.rs"
      provides: "Candidate queue ingestion from orchestration receipts"
      contains: "reflection-candidate queueing and candidate inspection payloads"
    - path: "crates/cli/src/commands/control.rs"
      provides: "Learning-candidate operator commands"
      contains: "approve, reject, supersede, and rollback flows"
    - path: "crates/cli/src/commands/start.rs"
      provides: "Runtime control and MCP review surfaces"
      contains: "candidate list, review, promotion, and rollback handlers"
  key_links:
    - from: "crates/cli/src/commands/orchestrate.rs"
      to: "crates/app/src/learning_review.rs"
      via: "queue reflection output into durable candidate review"
      pattern: "candidate|queue|promote"
    - from: "crates/cli/src/commands/start.rs"
      to: "crates/app/src/autonomy_lessons_control.rs"
      via: "approved candidate promotion returns lesson-aware reports"
      pattern: "lesson|candidate"
---

<objective>
Expose candidate review and bounded lesson promotion through the existing orchestration, CLI, and MCP control lanes.

Purpose: Phase 183 is not complete if the durable candidate queue exists but operators still have to bypass it or edit files manually.
Output: reflection-candidate queueing, operator review commands, MCP handlers, and rollback-aware lesson promotion wired into the shipped control path.
</objective>

<execution_context>
@/Users/hprincivil/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hprincivil/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-learning-candidate-review-and-lesson-promotion/183-CONTEXT.md
@.planning/phases/183-learning-candidate-review-and-lesson-promotion/183-RESEARCH.md
@crates/cli/src/commands/orchestrate.rs
@crates/cli/src/commands/control.rs
@crates/cli/src/commands/start.rs
@crates/cli/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-route reflection promotion into candidate review and add CLI review commands</name>
  <files>crates/cli/src/commands/orchestrate.rs, crates/cli/src/commands/control.rs, crates/cli/src/main.rs</files>
  <behavior>
    - Test 1: reflection-candidate promotion creates or returns a durable learning candidate instead of writing a lesson immediately.
    - Test 2: CLI review commands can approve, reject, supersede, and roll back candidates.
    - Test 3: approved promotion returns linked candidate and lesson identifiers.
  </behavior>
  <action>Replace the direct orchestration-to-lesson promotion path with candidate queueing through the new review service. Extend the existing control CLI with learning-candidate review commands and keep all promotion outputs bounded and provenance-rich.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli orchestrate -- --nocapture</automated>
    <automated>cargo test -p openrustclaw-cli control -- --nocapture</automated>
  </verify>
  <done>Operators can manage Phase 183 learning candidates from the standard CLI without bypassing review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend MCP and runtime control routes for candidate review and rollback</name>
  <files>crates/cli/src/commands/start.rs</files>
  <behavior>
    - Test 1: MCP handlers list and inspect candidates with evidence and review state.
    - Test 2: MCP mutation handlers can approve, reject, supersede, and roll back promoted candidates.
    - Test 3: promoted lesson rollback deactivates the linked lesson and updates candidate state durably.
  </behavior>
  <action>Add review-oriented runtime control handlers on the existing control lane so remote operators and tooling can inspect candidate evidence, perform review transitions, promote approved candidates, and roll back linked lessons without direct file edits.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli mcp_server_control_tools -- --nocapture</automated>
    <automated>cargo check -p openrustclaw-cli --tests</automated>
  </verify>
  <done>Phase 183 candidate review and bounded lesson promotion are available through the shipped MCP/control integration path.</done>
</task>

</tasks>

<verification>
- `cargo test -p openrustclaw-cli orchestrate -- --nocapture`
- `cargo test -p openrustclaw-cli control -- --nocapture`
- `cargo test -p openrustclaw-cli mcp_server_control_tools -- --nocapture`
- `cargo check -p openrustclaw-cli --tests`
</verification>
