# Plan 39-01

## Goal

Extract the control-auth and enterprise-access middleware slice from `start.rs` into a nested module.

## Steps

1. Create `crates/cli/src/commands/start/auth.rs`.
2. Move auth state, middleware, and token-loading helpers into that module.
3. Rewire `start.rs` to use the extracted functions without changing router behavior.

## Verification

- `cargo test -p openrustclaw-cli control_origin_validation -- --nocapture`
