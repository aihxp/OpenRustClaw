# Plan 97-01

## Goal

Extract onboarding, repair, and resume orchestration rules behind `openrustclaw-app`.

## Steps

1. Add an app-side setup lifecycle service with setup-profile defaults, step selection, repair-plan helpers, and handoff shaping.
2. Update `onboard.rs` to use the new service while keeping setup-state persistence, workspace reads, and runtime probes as adapter concerns.
3. Verify the extracted lane with app coverage and CLI library verification.
