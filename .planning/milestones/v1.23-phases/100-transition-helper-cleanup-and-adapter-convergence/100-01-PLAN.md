# Plan 100-01

## Goal

Reduce transition-era helper duplication and leave the affected secondary command modules with clearer adapter boundaries.

## Steps

1. Remove obsolete local helper logic from the migrated command modules once the corresponding app services exist.
2. Normalize the remaining conversion helpers so the migrated CLI modules stay focused on transport, persistence, and runtime side effects.
3. Verify that the cleanup preserves the shipped setup and secondary command contracts.
