# Plan 99-01

## Goal

Extract the targeted secondary operator-helper seams behind `openrustclaw-app`.

## Steps

1. Add app-side services for tool-host summaries, media support helpers, and memory view shaping.
2. Update `tools.rs`, `media.rs`, and `memory.rs` to use the new services while keeping artifact I/O, runtime calls, and workspace reads as adapter concerns.
3. Verify the extracted lane with app coverage and CLI library verification.
