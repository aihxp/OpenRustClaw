# Plan 98-01

## Goal

Extract the targeted secondary lifecycle command seams behind `openrustclaw-app`.

## Steps

1. Add app-side services for channel routing, schedule planning, channel health monitoring, and control-registry shaping.
2. Update `channels.rs`, `schedule.rs`, `services.rs`, and `control.rs` to use the new services while keeping metadata reads, persistence, and transport as adapter concerns.
3. Verify the extracted lane with app coverage and CLI library verification.
