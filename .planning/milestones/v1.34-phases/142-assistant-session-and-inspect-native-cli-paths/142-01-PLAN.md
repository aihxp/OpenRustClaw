# Plan 142-01

## Goal

Define the first assistant, session, and inspect native CLI operator-path slice so the implementation roadmap has a concrete user-facing successor path.

## Steps

1. Define the first implemented assistant, session, and inspect native CLI slice explicitly.
2. Tie that slice to the current operator-facing command hotspots instead of leaving them under general CLI replacement language.
3. Verify that the milestone leaves a concrete implementation path for the first native CLI operator families.
