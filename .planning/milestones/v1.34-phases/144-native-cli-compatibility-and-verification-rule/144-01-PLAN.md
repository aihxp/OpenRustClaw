# Plan 144-01

## Goal

Define the direct verification and compatibility rules for the first native CLI handoff slice so the second implementation milestone has a truthful source-level claim boundary.

## Steps

1. Define how the first native CLI successor paths are verified directly.
2. Define the compatibility rules that keep fallback routing ownership explicit and bounded.
3. Verify that the implementation roadmap advances to `2/6` only after the first native CLI slice is explicit end to end.
