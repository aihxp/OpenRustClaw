# Plan 141-01

## Goal

Define the first native CLI dispatch bootstrap slice so the implementation roadmap has a concrete successor routing path for top-level `openrustclaw` ownership.

## Steps

1. Define the first implemented native CLI dispatch slice explicitly.
2. Tie that slice to `crates/cli/src/main.rs` and `crates/cli/src/commands/mod.rs` instead of the legacy command tree as a whole.
3. Verify that the milestone leaves a concrete implementation path for the first CLI successor entrypoint.
