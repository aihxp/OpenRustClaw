# Plan 116-01

## Goal

Define the CLI boundary split and bounded compatibility-shim rules needed to implement the first native CLI slice without recreating another legacy-style command tree.

## Steps

1. Separate parsing, rendering, and app invocation responsibilities explicitly.
2. Define the allowed role for any temporary compatibility shims that remain in legacy CLI paths.
3. Verify that the roadmap only advances to `3/8` once the first CLI replacement slice is explicit end to end.
