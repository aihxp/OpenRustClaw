# Plan 146-01

## Goal

Define the runtime startup-boundary contracts needed for native runtime-host delivery so service-manager, probes, maintenance, and scheduler flows have explicit successor ownership.

## Steps

1. Map each runtime startup concern to a native owner and the contracts it depends on.
2. Replace legacy command-layer helper assumptions with explicit runtime-host boundary language in the roadmap.
3. Verify that the runtime-host story is concrete enough to implement without rediscovering startup ownership.
