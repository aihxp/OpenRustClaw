# Plan 145-01

## Goal

Define the first native runtime-host bootstrap slice so the implementation roadmap has a concrete successor startup path for service-manager, probes, maintenance, and scheduler ownership.

## Steps

1. Define the first implemented native runtime-host bootstrap slice explicitly.
2. Tie that slice to the real runtime startup hotspots instead of the legacy command layer as a whole.
3. Verify that the milestone leaves a concrete implementation path for the first runtime-host successor entrypoint.
