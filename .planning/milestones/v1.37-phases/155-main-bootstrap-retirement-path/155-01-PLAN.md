# Plan 155-01

## Goal

Define the bootstrap-retirement path for `main.rs` so the top-level binary stops depending on the legacy command tree as the real runtime path.

## Steps

1. Map the top-level binary concerns to native bootstrap and delivery owners.
2. Replace legacy command-tree bootstrap ownership with explicit native entrypoint handoff in the roadmap.
3. Verify that the milestone preserves a bounded migration story while bootstrap ownership changes hands.
