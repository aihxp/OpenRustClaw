---
phase: 170
plan: 01
type: execute
wave: 1
depends_on:
  - 169-01
files_modified:
  - ~/.cargo/credentials.toml
  - Cargo.toml
  - .planning/*
autonomous: true
user_setup: []
requirements:
  - PPC-04
must_haves:
  truths:
    - The public package is actually published, not just dry-run validated.
    - The repo tag line matches the shipped public version.
  artifacts:
    - path: dist/openrustclaw-0.1.1-x86_64-unknown-linux-gnu.tar.gz
      purpose: Built release artifact for the aligned public version
    - path: .planning/phases/170-release-execution-and-post-release-sync/170-VERIFICATION.md
      purpose: Records the actual publish and tag-alignment outcome
---

# Plan 170-01: Release execution and post-release sync

## Goal

Ship the `0.1.1` public release, verify the public package state, and align the repo’s public version line to the same version.

## Steps

1. Save valid crates.io credentials locally and publish `openrustclaw-core 0.1.1`.
2. Build the release artifact and align the repo to the same public version through a release commit and `v0.1.1` tag.
3. Verify that the package and repo version lines match the shipped result.
