---
phase: 168
plan: 01
type: execute
wave: 1
depends_on:
  - 167-01
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/e2e-tests.yml
  - scripts/check-security-audit.sh
  - scripts/check-runtime-budgets.sh
autonomous: true
user_setup: []
requirements:
  - PPC-03
must_haves:
  truths:
    - CI and release lanes are understandable from checked-in repo logic instead of hidden external defaults.
    - The repaired workflow surfaces have local parity checks proving the intended state.
  artifacts:
    - path: .github/workflows/ci.yml
      purpose: Repaired CI workflow with explicit security-audit behavior
    - path: scripts/check-security-audit.sh
      purpose: Repo-owned security audit policy and execution path
---

# Plan 168-01: GitHub Actions and release automation repair

## Goal

Make the relevant CI and release lanes understandable, reproducible, and green against the current shipped tree.

## Steps

1. Fix workflow trigger and gate configuration issues in the E2E and CI workflows.
2. Encode the project’s accepted audit exceptions in a checked-in script used by CI.
3. Verify the repaired lanes locally with the same commands the workflows now run.
