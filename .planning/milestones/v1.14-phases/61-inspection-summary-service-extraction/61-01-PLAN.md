# Plan 61-01: Migrate the Next Inspection Summary Family into the Application Lane

## Objective

Move self-hosted product-mode summary composition out of `inspect.rs` and into `openrustclaw-app`.

## Steps

1. Add a self-hosted product-mode service boundary to `openrustclaw-app`.
2. Replace the inline summary composition in `inspect.rs` with a bounded adapter that feeds the new service.
3. Preserve the existing runtime and Control UI report contract through focused regression tests.

## Verification

- `cargo test -p openrustclaw-app -- --nocapture`
- `cargo test -p openrustclaw-cli self_hosted_product_mode_summary -- --nocapture`
- `cargo test -p openrustclaw-cli dashboard_includes_self_hosted_product_mode_panel -- --nocapture`
