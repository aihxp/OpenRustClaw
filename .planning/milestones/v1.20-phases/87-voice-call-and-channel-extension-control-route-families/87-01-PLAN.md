---
wave: 1
---

# Plan 87-01: Extract the Voice-Call and Channel-Extension Control Route Family

## Objective

Move the remaining voice-call lifecycle and channel-extension control routes out of `start.rs` and behind one shared application-layer service boundary.

## Steps

1. Add an `openrustclaw-app` service for voice-call lifecycle and channel-extension control orchestration.
2. Replace the remaining direct voice-call and channel-extension handlers in `start.rs` with adapters into that shared service.
3. Verify the shipped route family with a deterministic runtime-router test that exercises binding, call lifecycle, and channel-extension flows.

## Verification

- `cargo test -p openrustclaw-app skill_voice_channel_control -- --nocapture`
- `cargo test -p openrustclaw-cli voice_call_and_channel_extension_route_family_uses_service_lane -- --nocapture`
