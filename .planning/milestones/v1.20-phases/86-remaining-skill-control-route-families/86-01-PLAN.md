---
wave: 1
---

# Plan 86-01: Extract the Remaining Skill-Control Route Family

## Objective

Move the remaining non-voice-call skill-control route families out of `start.rs` and behind one shared application-layer skill-control service.

## Steps

1. Finalize the `openrustclaw-app` request and service boundary for the remaining skill-control flows.
2. Keep `start.rs` as the HTTP adapter while routing compile, compiled-detail, background-service, invoke, execute, and adjacent skill-control handlers through the shared service lane.
3. Verify the shipped skill-control route family with a deterministic workspace skill fixture exercised through the runtime control router.

## Verification

- `cargo test -p openrustclaw-app skill_control -- --nocapture`
- `cargo test -p openrustclaw-cli remaining_skill_control_route_family_uses_service_lane -- --nocapture`
