---
wave: 1
---

# Plan 88-01: Clean Up Control-Plane Route Registration and Shared State

## Objective

Reduce `start.rs` route-registration and shared-state sprawl after the migrated control-plane route families have moved behind application services.

## Steps

1. Extract focused route-builder helpers for the migrated control-plane and runtime skill-control route families.
2. Simplify `control_plane_router` and `runtime_control_router` so those migrated families no longer depend on one oversized inline route chain.
3. Verify representative migrated routes still register and respond truthfully after the cleanup.

## Verification

- `cargo test -p openrustclaw-cli cleaned_up_route_registration_preserves_migrated_route_families -- --nocapture`
