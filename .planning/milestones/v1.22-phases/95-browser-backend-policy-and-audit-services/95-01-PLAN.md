# Plan 95-01

## Goal

Extract browser backend policy and audit handling behind `openrustclaw-app`.

## Steps

1. Add an app-side browser backend control service with policy normalization, backend-allowance evaluation, and audit-entry shaping.
2. Update `browser.rs` to use the new service while keeping audit-log persistence in the CLI adapter.
3. Verify the extracted policy lane with focused app-unit coverage and a clean CLI compile.
