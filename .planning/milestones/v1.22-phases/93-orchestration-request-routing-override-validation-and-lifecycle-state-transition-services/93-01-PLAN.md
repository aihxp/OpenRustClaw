# Plan 93-01

## Goal

Extract orchestration request routing, override validation, and lifecycle-state transition rules behind `openrustclaw-app`.

## Steps

1. Add an app-side orchestration routing service with route selection, autonomy override validation, and intervention-transition helpers.
2. Update `orchestrate.rs` to use the new service while keeping config loading, registry I/O, and active-run persistence as adapter concerns.
3. Verify the extracted lane with focused app-unit coverage and targeted orchestration command tests.
