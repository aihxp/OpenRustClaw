# Plan 96-01

## Goal

Extract browser session persistence and workflow bookkeeping behind `openrustclaw-app`.

## Steps

1. Add an app-side browser workflow service with session-record shaping, workflow-record composition, and history filtering.
2. Update `browser.rs` to use the new service while keeping browser automation and persistence I/O as adapter concerns.
3. Verify the extracted workflow lane with focused app-unit coverage and a clean CLI compile.
