# Plan 94-01

## Goal

Extract orchestration checkpoint, transcript, trace, reflection, and supervision summary composition behind `openrustclaw-app`.

## Steps

1. Add an app-side orchestration reporting service with trace-resource aggregation, reflection-candidate generation, and supervision payload builders.
2. Update `orchestrate.rs` to use the new reporting service while keeping run or active-run persistence as adapter concerns.
3. Verify the extracted reporting lane with focused app-unit coverage and a clean CLI compile.
