# Plan 101-01

## Goal

Remove the last targeted mixed-responsibility helper ownership from the remaining legacy command hotspots.

## Steps

1. Extract the targeted continuity, tool-execution audit, voice-call reporting, and compiled-skill MCP seams into `openrustclaw-app`.
2. Delete or replace the corresponding command-local helpers so the affected CLI modules lose business-rule ownership.
3. Verify that the migrated operator and runtime contracts still pass through the new services without behavior drift.
