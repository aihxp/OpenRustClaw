---
phase: 186-local-agent-discovery-and-compliance-inventory
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/app/src/agent_backend_catalog.rs
  - crates/app/src/lib.rs
  - crates/cli/src/commands/models.rs
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
must_haves:
  truths:
    - "OpenRustClaw has one shared app-layer catalog for supported local agent backends."
    - "The first shipped catalog surface exposes version, auth, readiness, and policy classification truthfully."
    - "Unsupported or ambiguous local agent tools stay visible with explicit readiness reasons."
  artifacts:
    - path: "crates/app/src/agent_backend_catalog.rs"
      provides: "Shared local-agent discovery and typed capability catalog"
      contains: "safe command probing, auth parsing, policy classification, readiness reasoning"
    - path: "crates/cli/src/commands/models.rs"
      provides: "First shipped operator-facing backend catalog surface"
      contains: "direct providers plus detected local agent backends"
  key_links:
    - from: "crates/cli/src/commands/models.rs"
      to: "crates/app/src/agent_backend_catalog.rs"
      via: "shared catalog service"
      pattern: "AgentBackendCatalogService::new().discover()"
---

<objective>
Ship the first truthful local-agent discovery slice for Phase 186.

Purpose: detect supported local agent binaries, classify readiness and auth truthfully, and surface the result through a shared app service plus one shipped operator-facing surface.
Output: a shared catalog service, detection and auth parsing for installed local agents, and an updated `openrustclaw models` surface that shows both direct providers and local agent backends.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/186-local-agent-discovery-and-compliance-inventory/186-CONTEXT.md
@crates/app/src/tool_host_service.rs
@crates/cli/src/commands/models.rs
@crates/cli/src/commands/onboard.rs
@crates/core/src/config.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared local-agent discovery catalog</name>
  <files>crates/app/src/agent_backend_catalog.rs, crates/app/src/lib.rs</files>
  <behavior>
    - Test 1: discovery reports installed fake `claude`, `codex`, `gemini`, and `cursor` binaries with truthful readiness states.
    - Test 2: Claude and Codex auth status probes are parsed into typed readiness metadata instead of raw strings.
    - Test 3: Cursor remains visible as detection-only instead of being mislabeled as a delegated backend.
  </behavior>
  <action>Create a shared app-layer catalog service that safely probes supported local binaries with `--help`, `--version`, and documented auth-status commands where available. Reuse existing host parsing utilities from `tool_host_service.rs`, keep the capability model typed, and preserve explicit notes or policy classification for ambiguous surfaces.</action>
  <verify>
    <automated>cargo test -p openrustclaw-app agent_backend_catalog -- --nocapture</automated>
  </verify>
  <done>The app layer exposes a reusable discovery catalog with truthful auth, readiness, and policy metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the shared catalog through the models surface</name>
  <files>crates/cli/src/commands/models.rs</files>
  <behavior>
    - Test 1: `models` still lists supported direct providers, including Gemini.
    - Test 2: `models` also shows detected local agent backends with readiness, auth, and policy-class information.
    - Test 3: the new surface uses the shared app catalog rather than another static local-agent list.
  </behavior>
  <action>Update `openrustclaw models` to keep direct providers visible while also rendering the shared local-agent discovery catalog. Add Gemini back to the direct provider list so the surface is more truthful immediately.</action>
  <verify>
    <automated>cargo test -p openrustclaw-cli models -- --nocapture</automated>
  </verify>
  <done>The models surface becomes the first shipped consumer of the shared local-agent discovery catalog.</done>
</task>

</tasks>

<verification>
- `cargo fmt --all`
- `cargo test -p openrustclaw-app agent_backend_catalog -- --nocapture`
- `cargo test -p openrustclaw-cli models -- --nocapture`
</verification>
