---
phase: 189-runtime-integration-and-agent-journey-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/runtime.rs
requirements:
  - ROUT-01
  - ROUT-03
must_haves:
  truths:
    - "Delegated local-agent backends can execute through the runtime provider factory as bounded non-interactive lanes."
    - "Delegated execution always evaluates existing external-backend policy before invocation."
    - "Delegated runtime calls leave durable audit and tool-execution receipts instead of disappearing into stdout."
---

<objective>
Add a bounded delegated CLI provider implementation for eligible local agent backends and wire it into the runtime provider factory.

Purpose: make delegated local-agent backends executable by the same runtime and orchestration plumbing that already handles direct providers.
</objective>

<context>
@.planning/phases/189-runtime-integration-and-agent-journey-delivery/189-CONTEXT.md
@crates/cli/src/commands/runtime.rs
@crates/app/src/agent_backend_control.rs
@crates/app/src/browser_backend_control.rs
@crates/cli/src/commands/inspect.rs
</context>

<verification>
- `cargo check -p openrustclaw-cli --tests`
- `cargo test -p openrustclaw-cli runtime -- --nocapture`
</verification>
