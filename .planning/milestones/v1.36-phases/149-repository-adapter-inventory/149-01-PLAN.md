# Plan 149-01

## Goal

Define the first repository-adapter inventory and successor ownership slice so the implementation roadmap has a concrete persistence handoff path for sqlite, workspace files, audit logs, runtime config, compiled-skill cache, and registry ownership.

## Steps

1. Define the first implemented repository-adapter inventory and successor slice explicitly.
2. Tie that slice to the real persistence-heavy command-local helper hotspots instead of the legacy command layer as a whole.
3. Verify that the milestone leaves a concrete implementation path for the first repository-adapter successor slice.
