---
phase: 07
plan: 03
type: execute
wave: 1
depends_on:
  - 07-01
  - 07-02
files_modified:
  - tests/e2e
  - tests/integration
  - scripts
autonomous: true
user_setup: []
requirements:
  - SEC-01
  - REL-01
must_haves:
  truths:
    - Release exit is backed by high-signal verification instead of docs alone.
    - The final gate reuses the shipped security and budget checks rather than a bespoke manual process.
    - MVP closeout has one concrete verification bundle for security, observability, and release constraints.
  artifacts:
    - path: tests/e2e
      purpose: Existing or augmented end-to-end security coverage for final release gate
    - path: tests/integration
      purpose: Final integration verification for typed security posture
    - path: scripts
      purpose: Release-gate command bundle or helper docs around existing scripts
---

# Phase 7 Plan 03: Lock the release exit with verification

## Objective
- **What:** Tie the final release gate to concrete automated checks.
- **Why:** The MVP cannot honestly claim release readiness if security posture and runtime budgets are only described, not exercised.
- **Output:** A final verification slice that reuses the best existing security and release checks.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-security-observability-and-release-exit/07-CONTEXT.md

## Tasks

<task type="code">
  <name>Add focused verification for the typed security posture</name>
  <files>tests/integration</files>
  <action>Create a realistic test that proves the shipped security posture summary exposes the final release-critical fields and warnings.</action>
  <verify>The test fails if the security posture surface loses critical release information.</verify>
  <done>The new security summary is covered.</done>
</task>

<task type="code">
  <name>Tie release exit to existing security and budget checks</name>
  <files>tests/e2e, scripts</files>
  <action>Reuse or wrap the existing security workflow tests and runtime budget script into a clear release-gate bundle.</action>
  <verify>The final release checks map directly to the documented checklist.</verify>
  <done>Release exit has a concrete verification path.</done>
</task>

## Verification
Run the focused security posture test plus the selected high-signal security and runtime-budget checks that define release readiness.

## Success Criteria
- [ ] Security posture surface is covered by automation.
- [ ] Release exit maps to concrete verification commands.
- [ ] The milestone closes with a real automated gate, not a narrative-only claim.
