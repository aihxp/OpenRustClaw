---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/security.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/control_ui.rs
autonomous: true
user_setup: []
requirements:
  - SEC-01
must_haves:
  truths:
    - Operators can inspect secure-default posture from a typed summary instead of a CLI-only audit dump.
    - Control UI exposes security posture alongside runtime posture and diagnostics.
    - Auth, origin validation, trusted-proxy, skill verification, and sandbox caveats are visible before release.
  artifacts:
    - path: crates/cli/src/commands/security.rs
      purpose: Compose a typed security posture summary and recommended actions
    - path: crates/cli/src/commands/start.rs
      purpose: Expose security posture through the control plane
    - path: crates/cli/src/commands/control_ui.html
      purpose: Browser operator surface for security posture
    - path: crates/cli/src/commands/control_ui.rs
      purpose: Dashboard regression test for the new security panel
---

# Phase 7 Plan 01: Add a unified security posture summary

## Objective
- **What:** Convert the existing security posture and audit signals into one typed summary visible from shipped operator surfaces.
- **Why:** Phase 7 cannot claim secure defaults if the operator has to infer them from scattered config values and CLI-only audit output.
- **Output:** A security posture summary exposed through the control plane and Control UI.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-security-observability-and-release-exit/07-CONTEXT.md

## Tasks

<task type="code">
  <name>Compose typed security posture data</name>
  <files>crates/cli/src/commands/security.rs</files>
  <action>Summarize auth, origin validation, trusted proxy, skill verification, and current sandbox caveats into one structured operator report.</action>
  <verify>The security contract is inspectable from typed data instead of colored CLI strings only.</verify>
  <done>Operators can inspect secure-default posture programmatically.</done>
</task>

<task type="code">
  <name>Expose security posture through the control plane</name>
  <files>crates/cli/src/commands/start.rs</files>
  <action>Add a control endpoint for the typed security posture summary.</action>
  <verify>The browser dashboard and any future operator client can consume the same security posture report.</verify>
  <done>Security posture is available through shipped control surfaces.</done>
</task>

<task type="code">
  <name>Surface the posture in Control UI</name>
  <files>crates/cli/src/commands/control_ui.html, crates/cli/src/commands/control_ui.rs</files>
  <action>Add a security posture panel to the dashboard and lock it with a UI test.</action>
  <verify>Operators can confirm secure-default posture without leaving the browser shell.</verify>
  <done>Control UI exposes security posture as a first-class trust surface.</done>
</task>

## Verification
Run narrow tests that prove the summary reports the expected fields and the Control UI renders the new posture panel.

## Success Criteria
- [ ] Security posture is available as typed data.
- [ ] Control UI surfaces the security contract clearly.
- [ ] Operators can see secure-default posture and warnings before release.
