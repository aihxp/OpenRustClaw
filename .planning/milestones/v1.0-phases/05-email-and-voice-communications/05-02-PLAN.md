---
phase: 05
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/cli/src/commands/voice_runtime.rs
  - crates/cli/src/commands/start.rs
  - crates/cli/src/commands/control_ui.html
  - tests/integration/src
autonomous: true
user_setup: []
requirements:
  - COMM-02
must_haves:
  truths:
    - Voice sessions and bounded voice-call flows expose stable lifecycle state, end reasons, and retained artifacts.
    - Operators can distinguish healthy, stale, ended, and failed communication runs from shipped inspection surfaces.
    - Voice runtime diagnostics remain workspace-bounded and durable instead of ephemeral.
  artifacts:
    - path: crates/cli/src/commands/voice_runtime.rs
      purpose: Harden persisted voice outcome and failure reporting
    - path: crates/cli/src/commands/start.rs
      purpose: Expose voice or call diagnostics through control endpoints
    - path: crates/cli/src/commands/control_ui.html
      purpose: Operator-facing review path for recent voice outcomes
    - path: tests/integration/src
      purpose: Regression coverage for durable voice communication receipts
  key_links:
    - from: crates/cli/src/commands/voice_runtime.rs
      to: crates/cli/src/commands/control_ui.html
      via: persisted voice session state becomes operator-visible communication diagnostics
---

# Phase 5 Plan 02: Harden the voice communication trust path

## Objective
- **What:** Tighten the voice-session and bounded voice-call operator story around lifecycle state, durable artifacts, and failure diagnosis.
- **Why:** The repo already ships rich voice capabilities, but Phase 5 needs a concise production-ready trust path instead of a pile of low-level controls.
- **Output:** Clearer persisted voice outcomes plus operator inspection surfaces that explain what happened and how to debug it.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-email-and-voice-communications/05-CONTEXT.md

## Tasks

<task type="code">
  <name>Normalize durable voice session outcome reporting</name>
  <files>crates/cli/src/commands/voice_runtime.rs</files>
  <action>Strengthen the persisted voice-session record or derived summaries so lifecycle state, failure detail, stale status, and artifact links are easier to trust and diagnose.</action>
  <verify>Operators can tell why a voice session ended or why it needs intervention without reconstructing state manually.</verify>
  <done>The voice runtime leaves behind clear communication receipts.</done>
</task>

<task type="code">
  <name>Expose voice communication diagnostics through the control plane</name>
  <files>crates/cli/src/commands/start.rs, crates/cli/src/commands/control_ui.html</files>
  <action>Add or refine control summaries so recent voice outcomes, health issues, and retained artifacts are easy to inspect from browser and API surfaces.</action>
  <verify>Recent voice communication state is legible from shipped operator surfaces.</verify>
  <done>The voice communication trust path is coherent for operators.</done>
</task>

<task type="code">
  <name>Back the voice trust path with automation</name>
  <files>tests/integration/src</files>
  <action>Add focused regression coverage for the durable voice communication contract.</action>
  <verify>Tests fail if persisted voice outcome visibility regresses.</verify>
  <done>Voice communications remain covered by automation as the runtime evolves.</done>
</task>

## Verification
Run targeted tests for persisted voice-session or bounded voice-call flows and confirm the retained state is visible through the intended operator surfaces.

## Success Criteria
- [ ] Voice communication outcomes remain durable and diagnosable.
- [ ] Operators can review recent voice state and artifacts from shipped control surfaces.
- [ ] Runtime health and failure paths are explicit enough for MVP support.
