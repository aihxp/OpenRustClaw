---
phase: 06
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - README.md
  - docs/src/getting-started/installation.md
  - docs/src/deployment/production.md
autonomous: true
user_setup: []
requirements:
  - OPS-01
must_haves:
  truths:
    - The production deployment and upgrade path is documented against the shipped Rust runtime commands.
    - Operators can follow one coherent runbook for install, restart, backup, upgrade, rollback, and diagnostics.
    - Top-level and deployment docs no longer leave recovery steps implied.
  artifacts:
    - path: README.md
      purpose: Top-level runtime-ops loop for operators
    - path: docs/src/getting-started/installation.md
      purpose: First production-capable install path with managed-service and recovery guidance
    - path: docs/src/deployment/production.md
      purpose: Canonical deploy-run-recover runbook aligned with shipped commands
---

# Phase 6 Plan 02: Align deployment and recovery docs with the shipped runtime

## Objective
- **What:** Rewrite the operator deployment and recovery guidance around the runtime commands that actually ship today.
- **Why:** The code already supports install, health, backup, upgrade, rollback, and lock inspection, but the docs still dilute that story with older or broader guidance.
- **Output:** A truthful production runbook that matches the Rust-first runtime path.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-deployment-runtime-and-operator-ops/06-CONTEXT.md

## Tasks

<task type="docs">
  <name>Document the canonical operator loop</name>
  <files>README.md</files>
  <action>Add a concise top-level deployment, restart, and recovery loop that points operators to the shipped runtime ops surfaces.</action>
  <verify>README gives a truthful starting point for production runtime operations.</verify>
  <done>Top-level docs no longer bury the MVP runtime-ops path.</done>
</task>

<task type="docs">
  <name>Update installation guidance for managed runtime service setup</name>
  <files>docs/src/getting-started/installation.md</files>
  <action>Show how the post-build install path flows into runtime service install, health verification, backup, and upgrade planning.</action>
  <verify>Operators can move from build to managed runtime operation without guessing the next command.</verify>
  <done>Installation docs bridge cleanly into Phase 6 operator ops.</done>
</task>

<task type="docs">
  <name>Rewrite production recovery guidance around shipped commands</name>
  <files>docs/src/deployment/production.md</files>
  <action>Align the production guide with the actual Rust runtime commands for service install, lock inspection, health, backup, config migration, upgrade, self-update, and rollback.</action>
  <verify>The deploy-run-recover runbook matches what the CLI and control plane actually support.</verify>
  <done>Production docs become a truthful operator recovery runbook.</done>
</task>

## Verification
Manual doc review plus command cross-checks should confirm every documented runtime-ops step maps to an existing shipped command or control surface.

## Success Criteria
- [ ] The canonical runtime deployment path is documented against the shipped Rust runtime.
- [ ] Upgrade and rollback guidance uses the real runtime commands.
- [ ] Installation and production docs describe one consistent operator story.
