---
phase: 02
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - crates/cli/src/commands/control_ui.html
  - crates/cli/src/commands/start.rs
  - tests/integration/src
autonomous: true
user_setup: []
requirements:
  - ASST-01
  - ASST-02
must_haves:
  truths:
    - Control UI shows assistant-first continuity information without requiring raw JSON inspection.
    - Operators can quickly distinguish resumed assistant sessions from generic stored sessions.
    - Control UI session rendering stays aligned with the typed control-session API.
  artifacts:
    - path: crates/cli/src/commands/control_ui.html
      purpose: Assistant continuity UX in the shipped operator dashboard
    - path: tests/integration/src
      purpose: Coverage for any continuity-specific control-session API or rendering helpers
  key_links:
    - from: crates/cli/src/commands/control_ui.html
      to: crates/cli/src/commands/inspect.rs
      via: UI renders the continuity summary produced by typed session inspection reports
---

# Phase 2 Plan 02: Surface continuity clearly in Control UI

## Objective
- **What:** Make the Control UI session panel assistant-first and continuity-aware.
- **Why:** Metadata exists today, but the operator dashboard does not yet make resumed-session trust obvious at a glance.
- **Output:** A clearer sessions table and session detail experience that foregrounds assistant continuity signals.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-assistant-and-session-continuity/02-CONTEXT.md
@.planning/phases/02-core-assistant-and-session-continuity/02-01-SUMMARY.md

## Tasks

<task type="code">
  <name>Render assistant continuity state in the sessions table</name>
  <files>crates/cli/src/commands/control_ui.html</files>
  <action>Update the session list view so assistant surface, continuity model, and resume hints are visible without opening raw detail JSON.</action>
  <verify>Operators can tell which sessions are primary assistant sessions from the table alone.</verify>
  <done>The sessions list becomes continuity-aware instead of generic storage browsing.</done>
</task>

<task type="code">
  <name>Render a concise continuity panel in session detail</name>
  <files>crates/cli/src/commands/control_ui.html</files>
  <action>Display the assistant continuity summary in a readable block before or alongside the raw inspection payload.</action>
  <verify>The first glance at session detail explains surface, route binding, and restored-history context clearly.</verify>
  <done>Session detail no longer requires operators to mentally decode metadata blobs.</done>
</task>

## Verification
Inspect the Control UI session screen against the typed API payload and confirm assistant continuity signals are visible in list and detail views.

## Success Criteria
- [ ] Sessions table identifies assistant-managed persisted sessions clearly.
- [ ] Session detail presents a concise continuity summary before raw JSON.
- [ ] Control UI remains aligned with the typed control-session report shape.
