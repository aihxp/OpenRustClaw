# Plan 59-01: Migrate the First Proving Slice into the Greenfield Lane

## Objective

Move setup handoff reporting out of direct CLI report composition and into the new application-layer service boundary.

## Steps

1. Wire `openrustclaw-cli` to depend on `openrustclaw-app`.
2. Replace the inline setup-handoff report composition in `inspect.rs` with a bounded adapter that feeds `openrustclaw-app::setup_handoff::SetupHandoffService`.
3. Keep the runtime route and dashboard behavior unchanged while existing regression tests verify the migrated path.

## Verification

- `cargo test -p openrustclaw-app -- --nocapture`
- `cargo test -p openrustclaw-cli setup_handoff_summary -- --nocapture`
- `cargo test -p openrustclaw-cli dashboard_includes_setup_handoff_panel -- --nocapture`
