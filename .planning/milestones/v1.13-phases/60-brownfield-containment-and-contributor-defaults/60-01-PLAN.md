# Plan 60-01: Lock Contributor Defaults and Brownfield Containment Rules

## Objective

Turn the greenfield lane from architecture intent into the default contribution contract for future work.

## Steps

1. Update contributor-facing docs to make `openrustclaw-app` the default home for new application logic.
2. Tighten brownfield containment rules and compatibility-only exceptions for the main legacy command hubs.
3. Record the next migration queue so follow-up work stays explicit after `v1.13` closes.

## Verification

- `cargo test -p openrustclaw-cli setup_handoff_summary -- --nocapture`
- `cargo test -p openrustclaw-cli dashboard_includes_setup_handoff_panel -- --nocapture`
- `mdbook build docs`
