# Plan 43-02: Repair the Parity Inventory Workflow

## Objective

Fix the CI contract so the public `Shipped Surface CI` workflow can run on GitHub-hosted runners without assuming `rg` is preinstalled.

## Steps

1. Install `ripgrep` explicitly in the parity-inventory job.
2. Add a `grep` fallback in `scripts/check-repo-hygiene.sh`.
3. Re-run the local parity verification bundle.
4. Push and observe the next GitHub Actions run.

## Verification

- local parity contract command bundle
- `bash scripts/check-repo-hygiene.sh`
- live `gh run list` / `gh run view`
