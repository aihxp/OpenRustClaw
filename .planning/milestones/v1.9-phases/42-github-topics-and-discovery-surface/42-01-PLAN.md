# Plan 42-01: Document the Canonical GitHub Topic Set

## Objective

Make the topic set explicit in the repo-admin docs instead of leaving it implicit in the JSON metadata file only.

## Steps

1. Add a canonical topic set section to `docs/github-repo-admin.md`.
2. Explain how topic changes should flow through `.github/repository-metadata.json`.
3. Keep the documentation grounded in the shipped product claim.

## Verification

- `bash scripts/github-repo-admin.sh validate-local`
