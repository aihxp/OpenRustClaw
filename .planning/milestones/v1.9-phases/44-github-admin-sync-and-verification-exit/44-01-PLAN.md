# Plan 44-01: Add GitHub Actions Admin Health Checks

## Objective

Give the repo one repeatable helper for inspecting workflow definitions, recent runs, and current `main` health.

## Steps

1. Add a GitHub Actions admin helper script.
2. Document the workflow-health commands in `docs/github-repo-admin.md`.
3. Keep the checks grounded in the public workflows that matter for the repo surface.

## Verification

- `bash scripts/github-actions-admin.sh workflows`
- `bash scripts/github-actions-admin.sh recent-runs`
