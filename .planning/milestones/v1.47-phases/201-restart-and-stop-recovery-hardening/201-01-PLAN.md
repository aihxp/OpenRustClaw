---
phase: 201-restart-and-stop-recovery-hardening
plan: "01"
type: execute
wave: 1
depends_on: [200-01]
files_modified:
  - crates/cli/src/commands/runtime.rs
  - crates/cli/src/commands/start.rs
autonomous: true
requirements: [RUN-01, RUN-03]
must_haves:
  truths:
    - `openrustclaw restart` fails with the classified listener-owner error before it launches a child runtime into a known port conflict.
    - `openrustclaw stop` clears stale runtime beacons as part of workspace-state reconciliation.
    - Recoverable OpenRustClaw listener ownership can still be stopped when the lock is gone but the runtime beacon proves the workspace owner.
  artifacts:
    - crates/cli/src/commands/runtime.rs
    - crates/cli/src/commands/start.rs
  key_links: []
---

<objective>
Harden `openrustclaw stop` and `openrustclaw restart` so listener reuse works reliably after stale workspace state and recoverable runtime ownership, and so restart no longer hides known listener conflicts behind a child-start early-exit message.
</objective>

<verification>
- `cargo test -p openrustclaw-cli stop_runtime_process_clears_stale_runtime_beacon_without_lock -- --nocapture`
- `cargo test -p openrustclaw-cli ensure_configured_listener_available_reports_foreign_process_conflict -- --nocapture`
- `cargo test -p openrustclaw-cli restart_runtime_process_reports_listener_conflict_before_launch -- --nocapture`
</verification>
