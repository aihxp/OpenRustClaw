---
phase: 200-runtime-ownership-and-conflict-classification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/commands/runtime.rs
  - crates/cli/src/commands/start.rs
autonomous: true
requirements: [RUN-02, DIAG-01, DIAG-02]
must_haves:
  truths:
    - `openrustclaw start` classifies busy listeners as active OpenRustClaw ownership, stale OpenRustClaw state, or a foreign-process conflict.
    - Listener ownership reporting is available directly from the bind failure path instead of forcing operators into manual process archaeology.
    - Startup no longer records a new runtime lock owner before the listener bind succeeds.
  artifacts:
    - crates/cli/src/commands/runtime.rs
    - crates/cli/src/commands/start.rs
  key_links: []
---

<objective>
Introduce listener ownership diagnosis for runtime lifecycle commands and wire that diagnosis into the `openrustclaw start` bind failure path so operators get a truthful conflict classification before the command exits.
</objective>

<verification>
- `cargo test -p openrustclaw-cli diagnose_listener_conflict_reports_active_runtime_lock_owner -- --nocapture`
- `cargo test -p openrustclaw-cli diagnose_listener_conflict_reports_stale_runtime_lock_owner -- --nocapture`
- `cargo test -p openrustclaw-cli bind_gateway_listener_reports_address_in_use_with_actionable_message -- --nocapture`
- `cargo test -p openrustclaw-cli bind_gateway_listener_reports_active_openrustclaw_runtime_owner -- --nocapture`
</verification>
