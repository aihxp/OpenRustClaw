# Plan 47-01: Validate Tag Publish Path End-to-End

## Objective

Prove that a successful tagged release run uploads the expected archives and checksums to GitHub Releases.

## Steps

1. Confirm the repaired workflow_dispatch build matrix is green for the supported targets.
2. Re-check the artifact naming and flattening path against `scripts/build-release-artifacts.sh`.
3. Use the final milestone tag run as the publish-path verification event.
4. Validate the tag-triggered `Publish GitHub Release Assets` job and resulting release assets with the admin helper.

## Verification

- `bash scripts/github-actions-admin.sh check-release-binaries main`
- `bash scripts/github-actions-admin.sh check-release-binaries v1.10`
- GitHub release assets attached to the `v1.10` release
