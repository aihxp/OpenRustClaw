# Documentation Contract

The source-of-truth documentation ownership contract lives in `docs/documentation-contract.md` at the repository root.

Use it to answer:

- which docs surface is canonical,
- which mdBook planning pages are mirrors,
- when a duplicate page should be merged or deleted,
- how future milestones keep docs in sync.

