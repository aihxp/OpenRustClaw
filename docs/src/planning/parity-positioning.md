# Parity Positioning

The source-of-truth positioning page lives in `docs/parity-positioning.md` at the repository root.

Use it to answer:

- what OpenRustClaw already matches well,
- where it is intentionally stronger than OpenClaw,
- where it still trails documented OpenClaw behavior,
- what does and does not count as a parity failure.
