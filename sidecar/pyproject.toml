[project]
name = "openrustclaw-sidecar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.4",
    "langchain>=0.3",
    "langsmith>=0.3",
    "grpcio>=1.60",
    "grpcio-tools>=1.60",
    "protobuf>=5.0",
]

[project.scripts]
openrustclaw-sidecar = "openrustclaw_sidecar.server:main"
